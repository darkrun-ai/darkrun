{
  "schema_version": 1,
  "run_slug": "dark-core",
  "factory": "software",
  "mode": "dark",
  "outcome": "sealed",
  "ticks": [
    {
      "seq": 0,
      "track": "run",
      "action_tag": "spec",
      "station": "frame",
      "prompt": "\n> **Run** `dark-core` · **Station** `frame` · **Phase** `spec`\n\n> Eliminates: _wrong-thing_\n\n\n# Spec — `frame`\n\nYou are opening station **frame**. Its job is to eliminate a whole class of risk: **wrong-thing**. Nothing downstream is allowed to proceed until that risk is named and bounded here.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`frame.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n\n\n**Explorers** (2): `context`, `value`\n\n\n**Workers** (3): `framer` → `challenger` → `distiller`\n\n\n**Reviewers** (2): `value`, `feasibility`\n\n\nSpec runs **elaboration and discovery in tandem** — they are NOT two sequential\nsteps. The moment the station opens, kick off both at once: dispatch the explorers\nin parallel *while* you frame the problem. They sharpen each other. Only once both\nhave landed do you decompose.\n\n\n\n## elaborate — frame the problem (concurrently with discovery)\n\nState plainly what this station must achieve to kill **wrong-thing**: the intent, the inputs it inherits from upstream, and the boundary of what is explicitly *out of scope* so later phases don't drift into it. This is the frame the explorers work against — but do NOT wait on a finished frame to start them; the frame and the exploration are written in parallel and inform each other.\n\n## discover — run the explorers in parallel (concurrently with elaboration)\n\nDispatch **all** explorers (`context`, `value`) **at once, in parallel** — one subagent each, fanned out concurrently, never one-after-another. Explorers don't build — they surface unknowns, constraints, prior art, and traps. They run alongside your framing; neither blocks the other.\n\n\n\nWhen discovery surfaces a durable project fact worth carrying into **future** runs — a constraint, prior art, a convention, a trap — persist it with **`darkrun_knowledge_record`** (`topic` + `body`). That's the project's shared memory; re-recording a topic updates it. Keep it project-level (cross-run truths), not this run's transient details.\n\n## decompose — once elaboration + discovery have both landed\n\nTurn the framed, explored problem into the smallest set of independently completable **Units** that, together, kill the risk above. A Unit's **body is the spec the executing subagent works from — it gets no other context**. A one-line body is a slug, not a definition; the work that comes back from a thin Unit is thin.\n\nWrite every Unit with `darkrun_unit_create`, with the full anatomy:\n\n- **`body`** — the real definition, in markdown:\n  - the goal: what this Unit produces and why it exists in this station,\n  - **completion criteria, EACH paired with the literal command that verifies it.** Inspect the project's manifest (`Cargo.toml` / `package.json` / `pyproject.toml` / `go.mod` …) *during decompose* and write commands against THIS project's actual stack — never a placeholder.\n    - Good: \"all endpoints return correct status codes (200/400/401/404)\" → `cargo test -p api contracts` exits 0.\n    - Bad: \"API works correctly\", \"tests are written\" — no check, no criterion.\n  - for build-class Units: the **success path, the failure path, and the edge cases** the criteria must cover,\n  - for knowledge/document Units: substantive criteria — what claims the artifact must ground, with sources,\n  - the **files touched** (so review knows the blast radius),\n  - what is explicitly **out of scope** (so the Unit doesn't sprawl).\n- **`depends_on`** — every cross-Unit prerequisite, DECLARED, never left in prose. The wave scheduler sequences **only** on `depends_on`; a dependency mentioned in the body but not declared is invisible — the Unit gets co-scheduled with its own prerequisite and handed inputs that don't exist yet. A body that says \"stub it until unit-X lands\" is the symptom of a missing `depends_on` edge: declare the edge instead of writing the stub.\n- **`inputs` / `outputs`** — the paths consumed and produced. A sibling-produced input path requires that sibling in `depends_on`.\n- **`quality_gates`** — executable `{name, command}` checks proving the criteria. Required for any Unit that declares outputs. Each gate must pass **in the Unit's own isolated worktree at the time it runs** — a gate that needs a sibling's unmerged code, with no `depends_on` edge to order it, is not a gate, it's a Unit scheduled to fail. Circular gates (zero-match `! grep`, prose substrings against the Unit's own output) are rejected.\n- **`model`** — match the tier to the risk: `opus` for architectural, cascading-failure, or deepest-reasoning work, `sonnet` (default) for known patterns plus judgment, `haiku` only for purely mechanical edits.\n\n\nThere are no Units yet. You are creating them.\n\n\n\n\n\n\n## Done when\n\nThe spec names the risk, lists Units with testable completion criteria and dependencies, marks what's out of scope, and it's written to the station's spec artifact. Then call `darkrun_tick`."
    },
    {
      "seq": 1,
      "track": "run",
      "action_tag": "review",
      "station": "frame",
      "prompt": "\n> **Run** `dark-core` · **Station** `frame` · **Phase** `review`\n\n> Eliminates: _wrong-thing_\n\n\n# Review — `frame` spec\n\nBefore a single Unit is manufactured, the spec gets reviewed. A bad spec that reaches manufacture is the most expensive failure in the line — kill it here, cheaply.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`frame.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n\n\n**Explorers** (2): `context`, `value`\n\n\n**Workers** (3): `framer` → `challenger` → `distiller`\n\n\n**Reviewers** (2): `value`, `feasibility`\n\n\nReview walks three beats, in order: **spec → adversarial → brief**. The operator's decision is a *separate* step the manager surfaces as a gate once this work lands — you do not ask for it inline here.\n\n## 1. spec — verify against the spec\n\nRead the spec produced in the previous phase against its own intent. Before any adversary touches it, confirm it is internally coherent:\n\n- Does it actually name and bound **wrong-thing**, or does it leave a hole?\n- Does every Unit carry testable completion criteria and explicit dependencies?\n- Is the out-of-scope boundary stated, so later phases can't drift into it?\n\n## 2. adversarial — adversarial reviewer pass\n\nDispatch each reviewer (`value`, `feasibility`) against the spec. Each owns one lens — let them be ruthless inside it:\n\n\n\n- Does the spec actually eliminate **wrong-thing**, or does it only look like it does?\n- Are the completion criteria testable, or are they wishful?\n- Are the Units genuinely independent, or will they collide during manufacture?\n- Is anything load-bearing left unstated?\n\n**Dispatch the reviewers in parallel** — one subagent each, fanned out concurrently, not one-after-another. They share no state and each owns a different lens, so they run independently. When a reviewer is satisfied it records its own sign-off with **`darkrun_review_stamp`** (`kind: review`, its `role`) — that stamps only its role and returns without advancing the run, so parallel reviewers never contend on the tick. A reviewer that finds a real problem files it with `darkrun_feedback_create` (origin `adversarial_review`) **instead of** stamping. You call `darkrun_tick` once, after every reviewer has returned.\n\n**A reviewer reviews — it does not redesign.** Each reviewer MUST NOT propose new requirements outside the spec's stated intent, MUST NOT substitute its own approach or relitigate a settled tradeoff, and MUST NOT block on stylistic preference. It finds where the spec fails *its own* goal and files exactly that — nothing more.\n\n\n### Units under review\n\n- `frame-unit`\n\n\n\nIf a reviewer blocks, fix the spec and re-review — do not advance a spec a reviewer rejected.\n\n## 3. brief — the review summary\n\nProduce a tight brief of the review: which lenses signed off, which filed concerns, and how each concern was resolved (or why it was deferred). This is the record manufacture inherits — it should make the spec's verdict obvious without re-reading every reviewer's notes.\n\nPersist it: call `darkrun_brief_record` with `slug: dark-core`, `station: frame`, `phase: pre`, and the brief as `body`. This is the pre-execution brief the operator's review gate surfaces — a durable artifact, not inline prose.\n\n## Done when\n\nEvery reviewer has signed off or filed addressable concerns and the brief is recorded with `darkrun_brief_record`. Then call `darkrun_tick` — the manager opens the operator's pre-execution gate on the next tick. Do not surface the decision inline; the gate does that."
    },
    {
      "seq": 2,
      "track": "run",
      "action_tag": "manufacture",
      "station": "frame",
      "prompt": "\n> **Run** `dark-core` · **Station** `frame` · **Phase** `manufacture`\n\n> Eliminates: _wrong-thing_\n\n\n# Manufacture — `frame`\n\nThis is the build floor. You run the **Pass loop** — _Plan → Make → Challenge → Resolve_ — over the wave-ready Units. The current beat is **framer**, on model **sonnet**.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`frame.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n\n\n**Explorers** (2): `context`, `value`\n\n\n**Workers** (3): `framer` → `challenger` → `distiller`\n\n\n**Reviewers** (2): `value`, `feasibility`\n\n\n## This wave\n\n\nDispatch the **framer** beat in parallel across these wave-ready Units:\n\n- `frame-unit`\n\n\n\n\n## Each Unit's spec — the contract the beat works against\n\nThe subagent you dispatch for a Unit gets **no context beyond what you hand it**. Pass the Unit's spec below into its dispatch verbatim — the completion criteria with their verify commands, the declared paths, and the scope boundary are the contract the beat is judged against.\n\n### `frame-unit` — frame-unit\n\n\n\n\n\n\n\n\n\n\n\n\n## The Pass loop — make → challenge → resolve\n\nThe Pass loop is adversarial on purpose: a single confident pass is exactly where LLM output is most often confidently wrong, so a second pass red-teams the first before anything locks.\n\n- **make** — the worker produces the Unit's output against its completion criteria. Build the real thing, not a sketch.\n- **challenge** — a second pass attacks what make produced: edge cases, missing handling, lazy assumptions. Assume the first pass was optimistic.\n- **resolve** — reconcile make and challenge into a Unit that satisfies its completion criteria with the challenges answered.\n\n\n\n\n**Quality-gate verifier nonce.** This dispatch carries a one-time verifier token: **`<nonce>`**. When you record a quality gate with `darkrun_quality_gate_record`, pass it as `nonce`. The engine refuses a gate result without the matching token — so a gate is only ever recorded as part of a real verification dispatch, never self-certified. Run the gate's command for real, then record the actual outcome with this nonce.\n\n\nRun **only the `framer` beat** this tick. When the beat finishes, **record it** with `darkrun_unit_iterate` — pass the `worker`, the `result` (`advance` or `reject`), and a `note`: on advance, what you did and what the next worker needs to know; on reject, why you bounced it (a reject without a reason is refused). That note becomes the next beat's handoff above. Then call `darkrun_tick`; the manager advances the loop or releases the next wave. A Unit is locked only after Resolve and its completion criteria pass.\n\nA Unit gets a **bounded pass budget** — the manager escalates a Unit that can't converge within it to the operator rather than grinding forever. Don't paper over a stuck Unit to dodge the escalation; a Unit that needs more passes than the budget allows is a signal the spec, the scope, or the approach is wrong, and that's the operator's call to make.\n\n\n\n## Done when\n\nThe `framer` beat is complete for every Unit in this wave and its output is recorded. Then call `darkrun_tick`."
    },
    {
      "seq": 3,
      "track": "run",
      "action_tag": "audit",
      "station": "frame",
      "prompt": "\n> **Run** `dark-core` · **Station** `frame` · **Phase** `audit`\n\n> Eliminates: _wrong-thing_\n\n\n# Audit — `frame` output\n\nThe Units are manufactured. Now audit the *output* against the *spec* **and run the quality checks**. Manufacture proves the thing was built; audit proves the *right* thing was built — and proves it with evidence, not just judgment. (Audit folds in what a separate tests phase used to do: reviewers give judgment, the checks give evidence. Both happen here.)\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`frame.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n\n\n**Explorers** (2): `context`, `value`\n\n\n**Workers** (3): `framer` → `challenger` → `distiller`\n\n\n**Reviewers** (2): `value`, `feasibility`\n\n\n## Sub-steps\n\nAudit walks two beats, in order:\n\n### 1. spec — verify against the locked spec\n\nDispatch the reviewers (`value`, `feasibility`) **in parallel** — one subagent each, concurrently — over the manufactured output. Each asks:\n\n- Does the output meet every Unit's **completion criteria** — not approximately, exactly?\n- Did manufacture drift from the locked spec? Any silent scope creep?\n- Does the combined output actually eliminate **wrong-thing**?\n- Is anything fragile, unhandled, or quietly broken?\n\nWhen a reviewer clears, it records its own approval with **`darkrun_review_stamp`** (`kind: approval`, its `role`) — stamping only its role without advancing the run, so the parallel pass never contends on the tick. A reviewer that finds a real problem files it with `darkrun_feedback_create` **instead of** stamping. You `darkrun_tick` once, after every reviewer returns.\n\n**A reviewer verifies — it does not rebuild.** Each reviewer MUST NOT propose new requirements beyond the Unit's completion criteria, MUST NOT redesign the output or reopen the locked spec, and MUST NOT flag stylistic preference. It checks the output against the criteria and files what genuinely fails — nothing more.\n\n\nUnits to audit:\n\n- `frame-unit`\n\n\n\n### 2. adversarial — adversarial reviewer pass + the checks\n\n- Run the full check suite for this station's output — tests, type checks, lints, builds, whatever this station's discipline demands. Run it **completely**, not a subset. A partial or skipped run is not a pass; green means green across the board.\n- If anything fails, it is yours to fix — failures here block the checkpoint. No \"pre-existing\" excuses; if it's red while this station owns the floor, fix it.\n- Adversarially attack the output: where would this break? What did every reviewer miss? Capture the real evidence.\n\n\n\n## The verdict — one state, no partial pass\n\nEvery check and every reviewer lands on exactly one verdict — never a partial, never a \"mostly\":\n\n- **PASS** — you ran it and *observed* it meet the criteria. Not \"looks right\", not \"should work\" — observed.\n- **FAIL** — you ran it and it does not meet the criteria. \"3 of 4 green\" is FAIL until the fourth is green or explicitly explained away.\n- **BLOCKED** — you could not run it. BLOCKED is **not** a PASS; the checkpoint holds until it can run.\n- **SKIP** — genuinely not applicable to this surface, with the reason stated.\n\nWhen in doubt, **FAIL** — a false PASS ships broken work; a false FAIL costs one more look.\n\n## Done when\n\nEvery reviewer has signed off or filed feedback, the full check suite passes, and the evidence is recorded against the station. Filed feedback becomes a fix-worker track — it does **not** get hand-waved past. Record the audit verdict (PASS only when every check passed and every reviewer cleared), then call `darkrun_tick`."
    },
    {
      "seq": 4,
      "track": "run",
      "action_tag": "reflect",
      "station": "frame",
      "prompt": "\n> **Run** `dark-core` · **Station** `frame` · **Phase** `reflect`\n\n> Eliminates: _wrong-thing_\n\n\n# Reflect — `frame`\n\nThe output is audited and the checks are green. Before the checkpoint gate fires, run an autonomous retrospective. This is the moment to capture what this station taught the run — the learnings that feed the run-level reflections.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`frame.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n## Sub-steps\n\n### agentic — autonomous reflection\n\nReflect on this station's pass, on your own, no human in the loop:\n\n- What did manufacturing **frame** reveal that the spec did not anticipate?\n- Where did the work fight back? What was harder, slower, or more fragile than expected?\n- What would you do differently next station? What pattern is worth carrying forward — or avoiding?\n- Did anything here eliminate **wrong-thing** more (or less) than expected?\n\n- What did the Units teach:\n\n  - `frame-unit`\n\n\n\nRecord the learnings with `darkrun_reflection_record` (pass the `frame` station and your retrospective as the `body`) so they persist on the run and inform later stations — read them back any time with `darkrun_reflection_list`. Be specific and honest — a vague reflection is a wasted one.\n\n## Done when\n\nThe retrospective is captured via `darkrun_reflection_record`. Then call `darkrun_tick` to reach the checkpoint."
    },
    {
      "seq": 5,
      "track": "run",
      "action_tag": "checkpoint",
      "station": "frame",
      "prompt": "\n> **Run** `dark-core` · **Station** `frame` · **Phase** `checkpoint`\n\n> Eliminates: _wrong-thing_\n\n\n# Checkpoint — `frame`\n\nStation **frame** has passed spec, review, manufacture, audit, and reflect. The gate is now open. Its kind is **`auto`** — that determines who decides whether the station locks.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`frame.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\nCheckpoint walks two beats, in order: **brief → user**.\n\n## 1. brief — produce the closing-brief summary\n\nWrite the tight closing brief for whoever holds the decision. It is the durable record of *why this station is allowed to lock*, so make it stand on its own:\n\n- What this station eliminated: **wrong-thing**.\n- The locked artifact (`frame.md`) and where the evidence lives — specs, audit verdict, the green check run.\n- Any concerns reviewers raised and how they were resolved.\n- The retrospective learnings reflect surfaced, if they bear on the lock.\n\nPersist it as the station's **outcome**: call `darkrun_brief_record` with `slug: dark-core`, `station: frame`, `phase: post`, and the closing brief as `body`. This is the durable \"what the station produced\" record the checkpoint surfaces — write it before clearing the gate.\n\n## 2. user — the gate decision (`auto`)\n\nThe gate kind decides *who* clears it. Surface the brief above, then act per the kind:\n\n\n**auto** — no human in the loop. The evidence already justifies the lock. Confirm the criteria are met, lock the station, and call `darkrun_tick` to advance.\n\n\n## Done when\n\nThe gate is cleared per its kind and the station is locked, or the run is held for a decision. Either way, call `darkrun_tick` to record the outcome."
    },
    {
      "seq": 6,
      "track": "run",
      "action_tag": "spec",
      "station": "specify",
      "prompt": "\n> **Run** `dark-core` · **Station** `specify` · **Phase** `spec`\n\n> Eliminates: _ambiguity_\n\n\n# Spec — `specify`\n\nYou are opening station **specify**. Its job is to eliminate a whole class of risk: **ambiguity**. Nothing downstream is allowed to proceed until that risk is named and bounded here.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`spec.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n\n\n**Explorers** (2): `contract`, `edge_case`\n\n\n**Workers** (3): `spec_writer` → `adversary` → `tightener`\n\n\n**Reviewers** (2): `testability`, `completeness`\n\n\nSpec runs **elaboration and discovery in tandem** — they are NOT two sequential\nsteps. The moment the station opens, kick off both at once: dispatch the explorers\nin parallel *while* you frame the problem. They sharpen each other. Only once both\nhave landed do you decompose.\n\n\n\n## elaborate — frame the problem (concurrently with discovery)\n\nState plainly what this station must achieve to kill **ambiguity**: the intent, the inputs it inherits from upstream, and the boundary of what is explicitly *out of scope* so later phases don't drift into it. This is the frame the explorers work against — but do NOT wait on a finished frame to start them; the frame and the exploration are written in parallel and inform each other.\n\n## discover — run the explorers in parallel (concurrently with elaboration)\n\nDispatch **all** explorers (`contract`, `edge_case`) **at once, in parallel** — one subagent each, fanned out concurrently, never one-after-another. Explorers don't build — they surface unknowns, constraints, prior art, and traps. They run alongside your framing; neither blocks the other.\n\n\n\nWhen discovery surfaces a durable project fact worth carrying into **future** runs — a constraint, prior art, a convention, a trap — persist it with **`darkrun_knowledge_record`** (`topic` + `body`). That's the project's shared memory; re-recording a topic updates it. Keep it project-level (cross-run truths), not this run's transient details.\n\n## decompose — once elaboration + discovery have both landed\n\nTurn the framed, explored problem into the smallest set of independently completable **Units** that, together, kill the risk above. A Unit's **body is the spec the executing subagent works from — it gets no other context**. A one-line body is a slug, not a definition; the work that comes back from a thin Unit is thin.\n\nWrite every Unit with `darkrun_unit_create`, with the full anatomy:\n\n- **`body`** — the real definition, in markdown:\n  - the goal: what this Unit produces and why it exists in this station,\n  - **completion criteria, EACH paired with the literal command that verifies it.** Inspect the project's manifest (`Cargo.toml` / `package.json` / `pyproject.toml` / `go.mod` …) *during decompose* and write commands against THIS project's actual stack — never a placeholder.\n    - Good: \"all endpoints return correct status codes (200/400/401/404)\" → `cargo test -p api contracts` exits 0.\n    - Bad: \"API works correctly\", \"tests are written\" — no check, no criterion.\n  - for build-class Units: the **success path, the failure path, and the edge cases** the criteria must cover,\n  - for knowledge/document Units: substantive criteria — what claims the artifact must ground, with sources,\n  - the **files touched** (so review knows the blast radius),\n  - what is explicitly **out of scope** (so the Unit doesn't sprawl).\n- **`depends_on`** — every cross-Unit prerequisite, DECLARED, never left in prose. The wave scheduler sequences **only** on `depends_on`; a dependency mentioned in the body but not declared is invisible — the Unit gets co-scheduled with its own prerequisite and handed inputs that don't exist yet. A body that says \"stub it until unit-X lands\" is the symptom of a missing `depends_on` edge: declare the edge instead of writing the stub.\n- **`inputs` / `outputs`** — the paths consumed and produced. A sibling-produced input path requires that sibling in `depends_on`.\n- **`quality_gates`** — executable `{name, command}` checks proving the criteria. Required for any Unit that declares outputs. Each gate must pass **in the Unit's own isolated worktree at the time it runs** — a gate that needs a sibling's unmerged code, with no `depends_on` edge to order it, is not a gate, it's a Unit scheduled to fail. Circular gates (zero-match `! grep`, prose substrings against the Unit's own output) are rejected.\n- **`model`** — match the tier to the risk: `opus` for architectural, cascading-failure, or deepest-reasoning work, `sonnet` (default) for known patterns plus judgment, `haiku` only for purely mechanical edits.\n\n\n### Units already on record\n\n- `specify-unit`\n\nReconcile these against what the explorers found — extend, split, or tighten them; don't blindly accept them.\n\n\n\n\n\n\n## Done when\n\nThe spec names the risk, lists Units with testable completion criteria and dependencies, marks what's out of scope, and it's written to the station's spec artifact. Then call `darkrun_tick`."
    },
    {
      "seq": 7,
      "track": "run",
      "action_tag": "review",
      "station": "specify",
      "prompt": "\n> **Run** `dark-core` · **Station** `specify` · **Phase** `review`\n\n> Eliminates: _ambiguity_\n\n\n# Review — `specify` spec\n\nBefore a single Unit is manufactured, the spec gets reviewed. A bad spec that reaches manufacture is the most expensive failure in the line — kill it here, cheaply.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`spec.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n\n\n**Explorers** (2): `contract`, `edge_case`\n\n\n**Workers** (3): `spec_writer` → `adversary` → `tightener`\n\n\n**Reviewers** (2): `testability`, `completeness`\n\n\nReview walks three beats, in order: **spec → adversarial → brief**. The operator's decision is a *separate* step the manager surfaces as a gate once this work lands — you do not ask for it inline here.\n\n## 1. spec — verify against the spec\n\nRead the spec produced in the previous phase against its own intent. Before any adversary touches it, confirm it is internally coherent:\n\n- Does it actually name and bound **ambiguity**, or does it leave a hole?\n- Does every Unit carry testable completion criteria and explicit dependencies?\n- Is the out-of-scope boundary stated, so later phases can't drift into it?\n\n## 2. adversarial — adversarial reviewer pass\n\nDispatch each reviewer (`testability`, `completeness`) against the spec. Each owns one lens — let them be ruthless inside it:\n\n\n\n- Does the spec actually eliminate **ambiguity**, or does it only look like it does?\n- Are the completion criteria testable, or are they wishful?\n- Are the Units genuinely independent, or will they collide during manufacture?\n- Is anything load-bearing left unstated?\n\n**Dispatch the reviewers in parallel** — one subagent each, fanned out concurrently, not one-after-another. They share no state and each owns a different lens, so they run independently. When a reviewer is satisfied it records its own sign-off with **`darkrun_review_stamp`** (`kind: review`, its `role`) — that stamps only its role and returns without advancing the run, so parallel reviewers never contend on the tick. A reviewer that finds a real problem files it with `darkrun_feedback_create` (origin `adversarial_review`) **instead of** stamping. You call `darkrun_tick` once, after every reviewer has returned.\n\n**A reviewer reviews — it does not redesign.** Each reviewer MUST NOT propose new requirements outside the spec's stated intent, MUST NOT substitute its own approach or relitigate a settled tradeoff, and MUST NOT block on stylistic preference. It finds where the spec fails *its own* goal and files exactly that — nothing more.\n\n\n### Units under review\n\n- `specify-unit`\n\n\n\nIf a reviewer blocks, fix the spec and re-review — do not advance a spec a reviewer rejected.\n\n## 3. brief — the review summary\n\nProduce a tight brief of the review: which lenses signed off, which filed concerns, and how each concern was resolved (or why it was deferred). This is the record manufacture inherits — it should make the spec's verdict obvious without re-reading every reviewer's notes.\n\nPersist it: call `darkrun_brief_record` with `slug: dark-core`, `station: specify`, `phase: pre`, and the brief as `body`. This is the pre-execution brief the operator's review gate surfaces — a durable artifact, not inline prose.\n\n## Done when\n\nEvery reviewer has signed off or filed addressable concerns and the brief is recorded with `darkrun_brief_record`. Then call `darkrun_tick` — the manager opens the operator's pre-execution gate on the next tick. Do not surface the decision inline; the gate does that."
    },
    {
      "seq": 8,
      "track": "run",
      "action_tag": "manufacture",
      "station": "specify",
      "prompt": "\n> **Run** `dark-core` · **Station** `specify` · **Phase** `manufacture`\n\n> Eliminates: _ambiguity_\n\n\n# Manufacture — `specify`\n\nThis is the build floor. You run the **Pass loop** — _Plan → Make → Challenge → Resolve_ — over the wave-ready Units. The current beat is **spec_writer**, on model **sonnet**.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`spec.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n\n\n**Explorers** (2): `contract`, `edge_case`\n\n\n**Workers** (3): `spec_writer` → `adversary` → `tightener`\n\n\n**Reviewers** (2): `testability`, `completeness`\n\n\n## This wave\n\n\nDispatch the **spec_writer** beat in parallel across these wave-ready Units:\n\n- `specify-unit`\n\n\n\n\n## Each Unit's spec — the contract the beat works against\n\nThe subagent you dispatch for a Unit gets **no context beyond what you hand it**. Pass the Unit's spec below into its dispatch verbatim — the completion criteria with their verify commands, the declared paths, and the scope boundary are the contract the beat is judged against.\n\n### `specify-unit` — specify-unit\n\n- **inputs:** `frame.md`\n\n\n\n\n\n\n\n\n\n\n\n\n## The Pass loop — make → challenge → resolve\n\nThe Pass loop is adversarial on purpose: a single confident pass is exactly where LLM output is most often confidently wrong, so a second pass red-teams the first before anything locks.\n\n- **make** — the worker produces the Unit's output against its completion criteria. Build the real thing, not a sketch.\n- **challenge** — a second pass attacks what make produced: edge cases, missing handling, lazy assumptions. Assume the first pass was optimistic.\n- **resolve** — reconcile make and challenge into a Unit that satisfies its completion criteria with the challenges answered.\n\n\n\n\n**Quality-gate verifier nonce.** This dispatch carries a one-time verifier token: **`<nonce>`**. When you record a quality gate with `darkrun_quality_gate_record`, pass it as `nonce`. The engine refuses a gate result without the matching token — so a gate is only ever recorded as part of a real verification dispatch, never self-certified. Run the gate's command for real, then record the actual outcome with this nonce.\n\n\nRun **only the `spec_writer` beat** this tick. When the beat finishes, **record it** with `darkrun_unit_iterate` — pass the `worker`, the `result` (`advance` or `reject`), and a `note`: on advance, what you did and what the next worker needs to know; on reject, why you bounced it (a reject without a reason is refused). That note becomes the next beat's handoff above. Then call `darkrun_tick`; the manager advances the loop or releases the next wave. A Unit is locked only after Resolve and its completion criteria pass.\n\nA Unit gets a **bounded pass budget** — the manager escalates a Unit that can't converge within it to the operator rather than grinding forever. Don't paper over a stuck Unit to dodge the escalation; a Unit that needs more passes than the budget allows is a signal the spec, the scope, or the approach is wrong, and that's the operator's call to make.\n\n\n\n## Done when\n\nThe `spec_writer` beat is complete for every Unit in this wave and its output is recorded. Then call `darkrun_tick`."
    },
    {
      "seq": 9,
      "track": "run",
      "action_tag": "audit",
      "station": "specify",
      "prompt": "\n> **Run** `dark-core` · **Station** `specify` · **Phase** `audit`\n\n> Eliminates: _ambiguity_\n\n\n# Audit — `specify` output\n\nThe Units are manufactured. Now audit the *output* against the *spec* **and run the quality checks**. Manufacture proves the thing was built; audit proves the *right* thing was built — and proves it with evidence, not just judgment. (Audit folds in what a separate tests phase used to do: reviewers give judgment, the checks give evidence. Both happen here.)\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`spec.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n\n\n**Explorers** (2): `contract`, `edge_case`\n\n\n**Workers** (3): `spec_writer` → `adversary` → `tightener`\n\n\n**Reviewers** (2): `testability`, `completeness`\n\n\n## Sub-steps\n\nAudit walks two beats, in order:\n\n### 1. spec — verify against the locked spec\n\nDispatch the reviewers (`testability`, `completeness`) **in parallel** — one subagent each, concurrently — over the manufactured output. Each asks:\n\n- Does the output meet every Unit's **completion criteria** — not approximately, exactly?\n- Did manufacture drift from the locked spec? Any silent scope creep?\n- Does the combined output actually eliminate **ambiguity**?\n- Is anything fragile, unhandled, or quietly broken?\n\nWhen a reviewer clears, it records its own approval with **`darkrun_review_stamp`** (`kind: approval`, its `role`) — stamping only its role without advancing the run, so the parallel pass never contends on the tick. A reviewer that finds a real problem files it with `darkrun_feedback_create` **instead of** stamping. You `darkrun_tick` once, after every reviewer returns.\n\n**A reviewer verifies — it does not rebuild.** Each reviewer MUST NOT propose new requirements beyond the Unit's completion criteria, MUST NOT redesign the output or reopen the locked spec, and MUST NOT flag stylistic preference. It checks the output against the criteria and files what genuinely fails — nothing more.\n\n\nUnits to audit:\n\n- `specify-unit`\n\n\n\n### 2. adversarial — adversarial reviewer pass + the checks\n\n- Run the full check suite for this station's output — tests, type checks, lints, builds, whatever this station's discipline demands. Run it **completely**, not a subset. A partial or skipped run is not a pass; green means green across the board.\n- If anything fails, it is yours to fix — failures here block the checkpoint. No \"pre-existing\" excuses; if it's red while this station owns the floor, fix it.\n- Adversarially attack the output: where would this break? What did every reviewer miss? Capture the real evidence.\n\n\n\n## The verdict — one state, no partial pass\n\nEvery check and every reviewer lands on exactly one verdict — never a partial, never a \"mostly\":\n\n- **PASS** — you ran it and *observed* it meet the criteria. Not \"looks right\", not \"should work\" — observed.\n- **FAIL** — you ran it and it does not meet the criteria. \"3 of 4 green\" is FAIL until the fourth is green or explicitly explained away.\n- **BLOCKED** — you could not run it. BLOCKED is **not** a PASS; the checkpoint holds until it can run.\n- **SKIP** — genuinely not applicable to this surface, with the reason stated.\n\nWhen in doubt, **FAIL** — a false PASS ships broken work; a false FAIL costs one more look.\n\n## Done when\n\nEvery reviewer has signed off or filed feedback, the full check suite passes, and the evidence is recorded against the station. Filed feedback becomes a fix-worker track — it does **not** get hand-waved past. Record the audit verdict (PASS only when every check passed and every reviewer cleared), then call `darkrun_tick`."
    },
    {
      "seq": 10,
      "track": "run",
      "action_tag": "reflect",
      "station": "specify",
      "prompt": "\n> **Run** `dark-core` · **Station** `specify` · **Phase** `reflect`\n\n> Eliminates: _ambiguity_\n\n\n# Reflect — `specify`\n\nThe output is audited and the checks are green. Before the checkpoint gate fires, run an autonomous retrospective. This is the moment to capture what this station taught the run — the learnings that feed the run-level reflections.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`spec.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n## Sub-steps\n\n### agentic — autonomous reflection\n\nReflect on this station's pass, on your own, no human in the loop:\n\n- What did manufacturing **specify** reveal that the spec did not anticipate?\n- Where did the work fight back? What was harder, slower, or more fragile than expected?\n- What would you do differently next station? What pattern is worth carrying forward — or avoiding?\n- Did anything here eliminate **ambiguity** more (or less) than expected?\n\n- What did the Units teach:\n\n  - `specify-unit`\n\n\n\nRecord the learnings with `darkrun_reflection_record` (pass the `specify` station and your retrospective as the `body`) so they persist on the run and inform later stations — read them back any time with `darkrun_reflection_list`. Be specific and honest — a vague reflection is a wasted one.\n\n## Done when\n\nThe retrospective is captured via `darkrun_reflection_record`. Then call `darkrun_tick` to reach the checkpoint."
    },
    {
      "seq": 11,
      "track": "run",
      "action_tag": "checkpoint",
      "station": "specify",
      "prompt": "\n> **Run** `dark-core` · **Station** `specify` · **Phase** `checkpoint`\n\n> Eliminates: _ambiguity_\n\n\n# Checkpoint — `specify`\n\nStation **specify** has passed spec, review, manufacture, audit, and reflect. The gate is now open. Its kind is **`auto`** — that determines who decides whether the station locks.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`spec.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\nCheckpoint walks two beats, in order: **brief → user**.\n\n## 1. brief — produce the closing-brief summary\n\nWrite the tight closing brief for whoever holds the decision. It is the durable record of *why this station is allowed to lock*, so make it stand on its own:\n\n- What this station eliminated: **ambiguity**.\n- The locked artifact (`spec.md`) and where the evidence lives — specs, audit verdict, the green check run.\n- Any concerns reviewers raised and how they were resolved.\n- The retrospective learnings reflect surfaced, if they bear on the lock.\n\nPersist it as the station's **outcome**: call `darkrun_brief_record` with `slug: dark-core`, `station: specify`, `phase: post`, and the closing brief as `body`. This is the durable \"what the station produced\" record the checkpoint surfaces — write it before clearing the gate.\n\n## 2. user — the gate decision (`auto`)\n\nThe gate kind decides *who* clears it. Surface the brief above, then act per the kind:\n\n\n**auto** — no human in the loop. The evidence already justifies the lock. Confirm the criteria are met, lock the station, and call `darkrun_tick` to advance.\n\n\n## Done when\n\nThe gate is cleared per its kind and the station is locked, or the run is held for a decision. Either way, call `darkrun_tick` to record the outcome."
    },
    {
      "seq": 12,
      "track": "run",
      "action_tag": "spec",
      "station": "shape",
      "prompt": "\n> **Run** `dark-core` · **Station** `shape` · **Phase** `spec`\n\n> Eliminates: _expensive-structural-reversal_\n\n\n# Spec — `shape`\n\nYou are opening station **shape**. Its job is to eliminate a whole class of risk: **expensive-structural-reversal**. Nothing downstream is allowed to proceed until that risk is named and bounded here.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`design.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n\n\n**Explorers** (3): `surface`, `architecture`, `risk`\n\n\n**Workers** (5): `designer` → `visual_designer` → `spiker` → `pressure_tester` → `resolver`\n\n\n**Reviewers** (3): `fit`, `reversibility`, `simplicity`\n\n\nSpec runs **elaboration and discovery in tandem** — they are NOT two sequential\nsteps. The moment the station opens, kick off both at once: dispatch the explorers\nin parallel *while* you frame the problem. They sharpen each other. Only once both\nhave landed do you decompose.\n\n\n## Keep or drop — decide at arrival, before any work\n\nThis station is **optional** for this run. Before you elaborate anything, judge whether its risk class — **expensive-structural-reversal** — actually applies here. If it plainly doesn't (the run is too small to carry the risk, or an upstream artifact already bounds it), drop the station now with `darkrun_station_drop` and the next `darkrun_tick` advances to the following station. The decision is only available **now**: once elaboration or units exist the station has started, and a started station can only be reset, never dropped. Keeping it is the default — drop only when you can say in one sentence why the risk doesn't apply.\n\n\n## elaborate — frame the problem (concurrently with discovery)\n\nState plainly what this station must achieve to kill **expensive-structural-reversal**: the intent, the inputs it inherits from upstream, and the boundary of what is explicitly *out of scope* so later phases don't drift into it. This is the frame the explorers work against — but do NOT wait on a finished frame to start them; the frame and the exploration are written in parallel and inform each other.\n\n## discover — run the explorers in parallel (concurrently with elaboration)\n\nDispatch **all** explorers (`surface`, `architecture`, `risk`) **at once, in parallel** — one subagent each, fanned out concurrently, never one-after-another. Explorers don't build — they surface unknowns, constraints, prior art, and traps. They run alongside your framing; neither blocks the other.\n\n\n\nWhen discovery surfaces a durable project fact worth carrying into **future** runs — a constraint, prior art, a convention, a trap — persist it with **`darkrun_knowledge_record`** (`topic` + `body`). That's the project's shared memory; re-recording a topic updates it. Keep it project-level (cross-run truths), not this run's transient details.\n\n## decompose — once elaboration + discovery have both landed\n\nTurn the framed, explored problem into the smallest set of independently completable **Units** that, together, kill the risk above. A Unit's **body is the spec the executing subagent works from — it gets no other context**. A one-line body is a slug, not a definition; the work that comes back from a thin Unit is thin.\n\nWrite every Unit with `darkrun_unit_create`, with the full anatomy:\n\n- **`body`** — the real definition, in markdown:\n  - the goal: what this Unit produces and why it exists in this station,\n  - **completion criteria, EACH paired with the literal command that verifies it.** Inspect the project's manifest (`Cargo.toml` / `package.json` / `pyproject.toml` / `go.mod` …) *during decompose* and write commands against THIS project's actual stack — never a placeholder.\n    - Good: \"all endpoints return correct status codes (200/400/401/404)\" → `cargo test -p api contracts` exits 0.\n    - Bad: \"API works correctly\", \"tests are written\" — no check, no criterion.\n  - for build-class Units: the **success path, the failure path, and the edge cases** the criteria must cover,\n  - for knowledge/document Units: substantive criteria — what claims the artifact must ground, with sources,\n  - the **files touched** (so review knows the blast radius),\n  - what is explicitly **out of scope** (so the Unit doesn't sprawl).\n- **`depends_on`** — every cross-Unit prerequisite, DECLARED, never left in prose. The wave scheduler sequences **only** on `depends_on`; a dependency mentioned in the body but not declared is invisible — the Unit gets co-scheduled with its own prerequisite and handed inputs that don't exist yet. A body that says \"stub it until unit-X lands\" is the symptom of a missing `depends_on` edge: declare the edge instead of writing the stub.\n- **`inputs` / `outputs`** — the paths consumed and produced. A sibling-produced input path requires that sibling in `depends_on`.\n- **`quality_gates`** — executable `{name, command}` checks proving the criteria. Required for any Unit that declares outputs. Each gate must pass **in the Unit's own isolated worktree at the time it runs** — a gate that needs a sibling's unmerged code, with no `depends_on` edge to order it, is not a gate, it's a Unit scheduled to fail. Circular gates (zero-match `! grep`, prose substrings against the Unit's own output) are rejected.\n- **`model`** — match the tier to the risk: `opus` for architectural, cascading-failure, or deepest-reasoning work, `sonnet` (default) for known patterns plus judgment, `haiku` only for purely mechanical edits.\n\n\n### Units already on record\n\n- `shape-unit`\n\nReconcile these against what the explorers found — extend, split, or tighten them; don't blindly accept them.\n\n\n\n\n\n\n## Done when\n\nThe spec names the risk, lists Units with testable completion criteria and dependencies, marks what's out of scope, and it's written to the station's spec artifact. Then call `darkrun_tick`."
    },
    {
      "seq": 13,
      "track": "run",
      "action_tag": "review",
      "station": "shape",
      "prompt": "\n> **Run** `dark-core` · **Station** `shape` · **Phase** `review`\n\n> Eliminates: _expensive-structural-reversal_\n\n\n# Review — `shape` spec\n\nBefore a single Unit is manufactured, the spec gets reviewed. A bad spec that reaches manufacture is the most expensive failure in the line — kill it here, cheaply.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`design.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n\n\n**Explorers** (3): `surface`, `architecture`, `risk`\n\n\n**Workers** (5): `designer` → `visual_designer` → `spiker` → `pressure_tester` → `resolver`\n\n\n**Reviewers** (3): `fit`, `reversibility`, `simplicity`\n\n\nReview walks three beats, in order: **spec → adversarial → brief**. The operator's decision is a *separate* step the manager surfaces as a gate once this work lands — you do not ask for it inline here.\n\n## 1. spec — verify against the spec\n\nRead the spec produced in the previous phase against its own intent. Before any adversary touches it, confirm it is internally coherent:\n\n- Does it actually name and bound **expensive-structural-reversal**, or does it leave a hole?\n- Does every Unit carry testable completion criteria and explicit dependencies?\n- Is the out-of-scope boundary stated, so later phases can't drift into it?\n\n## 2. adversarial — adversarial reviewer pass\n\nDispatch each reviewer (`fit`, `reversibility`, `simplicity`) against the spec. Each owns one lens — let them be ruthless inside it:\n\n\n\n- Does the spec actually eliminate **expensive-structural-reversal**, or does it only look like it does?\n- Are the completion criteria testable, or are they wishful?\n- Are the Units genuinely independent, or will they collide during manufacture?\n- Is anything load-bearing left unstated?\n\n**Dispatch the reviewers in parallel** — one subagent each, fanned out concurrently, not one-after-another. They share no state and each owns a different lens, so they run independently. When a reviewer is satisfied it records its own sign-off with **`darkrun_review_stamp`** (`kind: review`, its `role`) — that stamps only its role and returns without advancing the run, so parallel reviewers never contend on the tick. A reviewer that finds a real problem files it with `darkrun_feedback_create` (origin `adversarial_review`) **instead of** stamping. You call `darkrun_tick` once, after every reviewer has returned.\n\n**A reviewer reviews — it does not redesign.** Each reviewer MUST NOT propose new requirements outside the spec's stated intent, MUST NOT substitute its own approach or relitigate a settled tradeoff, and MUST NOT block on stylistic preference. It finds where the spec fails *its own* goal and files exactly that — nothing more.\n\n\n### Units under review\n\n- `shape-unit`\n\n\n\nIf a reviewer blocks, fix the spec and re-review — do not advance a spec a reviewer rejected.\n\n## 3. brief — the review summary\n\nProduce a tight brief of the review: which lenses signed off, which filed concerns, and how each concern was resolved (or why it was deferred). This is the record manufacture inherits — it should make the spec's verdict obvious without re-reading every reviewer's notes.\n\nPersist it: call `darkrun_brief_record` with `slug: dark-core`, `station: shape`, `phase: pre`, and the brief as `body`. This is the pre-execution brief the operator's review gate surfaces — a durable artifact, not inline prose.\n\n## Done when\n\nEvery reviewer has signed off or filed addressable concerns and the brief is recorded with `darkrun_brief_record`. Then call `darkrun_tick` — the manager opens the operator's pre-execution gate on the next tick. Do not surface the decision inline; the gate does that."
    },
    {
      "seq": 14,
      "track": "run",
      "action_tag": "manufacture",
      "station": "shape",
      "prompt": "\n> **Run** `dark-core` · **Station** `shape` · **Phase** `manufacture`\n\n> Eliminates: _expensive-structural-reversal_\n\n\n# Manufacture — `shape`\n\nThis is the build floor. You run the **Pass loop** — _Plan → Make → Challenge → Resolve_ — over the wave-ready Units. The current beat is **designer**, on model **sonnet**.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`design.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n\n\n**Explorers** (3): `surface`, `architecture`, `risk`\n\n\n**Workers** (5): `designer` → `visual_designer` → `spiker` → `pressure_tester` → `resolver`\n\n\n**Reviewers** (3): `fit`, `reversibility`, `simplicity`\n\n\n## This wave\n\n\nDispatch the **designer** beat in parallel across these wave-ready Units:\n\n- `shape-unit`\n\n\n\n\n## Each Unit's spec — the contract the beat works against\n\nThe subagent you dispatch for a Unit gets **no context beyond what you hand it**. Pass the Unit's spec below into its dispatch verbatim — the completion criteria with their verify commands, the declared paths, and the scope boundary are the contract the beat is judged against.\n\n### `shape-unit` — shape-unit\n\n- **inputs:** `frame.md`, `spec.md`\n\n\n\n\n\n\n\n\n\n\n\n\n## The Pass loop — make → challenge → resolve\n\nThe Pass loop is adversarial on purpose: a single confident pass is exactly where LLM output is most often confidently wrong, so a second pass red-teams the first before anything locks.\n\n- **make** — the worker produces the Unit's output against its completion criteria. Build the real thing, not a sketch.\n- **challenge** — a second pass attacks what make produced: edge cases, missing handling, lazy assumptions. Assume the first pass was optimistic.\n- **resolve** — reconcile make and challenge into a Unit that satisfies its completion criteria with the challenges answered.\n\n\n\n\n**Quality-gate verifier nonce.** This dispatch carries a one-time verifier token: **`<nonce>`**. When you record a quality gate with `darkrun_quality_gate_record`, pass it as `nonce`. The engine refuses a gate result without the matching token — so a gate is only ever recorded as part of a real verification dispatch, never self-certified. Run the gate's command for real, then record the actual outcome with this nonce.\n\n\nRun **only the `designer` beat** this tick. When the beat finishes, **record it** with `darkrun_unit_iterate` — pass the `worker`, the `result` (`advance` or `reject`), and a `note`: on advance, what you did and what the next worker needs to know; on reject, why you bounced it (a reject without a reason is refused). That note becomes the next beat's handoff above. Then call `darkrun_tick`; the manager advances the loop or releases the next wave. A Unit is locked only after Resolve and its completion criteria pass.\n\nA Unit gets a **bounded pass budget** — the manager escalates a Unit that can't converge within it to the operator rather than grinding forever. Don't paper over a stuck Unit to dodge the escalation; a Unit that needs more passes than the budget allows is a signal the spec, the scope, or the approach is wrong, and that's the operator's call to make.\n\n\n\n## Done when\n\nThe `designer` beat is complete for every Unit in this wave and its output is recorded. Then call `darkrun_tick`."
    },
    {
      "seq": 15,
      "track": "run",
      "action_tag": "audit",
      "station": "shape",
      "prompt": "\n> **Run** `dark-core` · **Station** `shape` · **Phase** `audit`\n\n> Eliminates: _expensive-structural-reversal_\n\n\n# Audit — `shape` output\n\nThe Units are manufactured. Now audit the *output* against the *spec* **and run the quality checks**. Manufacture proves the thing was built; audit proves the *right* thing was built — and proves it with evidence, not just judgment. (Audit folds in what a separate tests phase used to do: reviewers give judgment, the checks give evidence. Both happen here.)\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`design.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n\n\n**Explorers** (3): `surface`, `architecture`, `risk`\n\n\n**Workers** (5): `designer` → `visual_designer` → `spiker` → `pressure_tester` → `resolver`\n\n\n**Reviewers** (3): `fit`, `reversibility`, `simplicity`\n\n\n## Sub-steps\n\nAudit walks two beats, in order:\n\n### 1. spec — verify against the locked spec\n\nDispatch the reviewers (`fit`, `reversibility`, `simplicity`) **in parallel** — one subagent each, concurrently — over the manufactured output. Each asks:\n\n- Does the output meet every Unit's **completion criteria** — not approximately, exactly?\n- Did manufacture drift from the locked spec? Any silent scope creep?\n- Does the combined output actually eliminate **expensive-structural-reversal**?\n- Is anything fragile, unhandled, or quietly broken?\n\nWhen a reviewer clears, it records its own approval with **`darkrun_review_stamp`** (`kind: approval`, its `role`) — stamping only its role without advancing the run, so the parallel pass never contends on the tick. A reviewer that finds a real problem files it with `darkrun_feedback_create` **instead of** stamping. You `darkrun_tick` once, after every reviewer returns.\n\n**A reviewer verifies — it does not rebuild.** Each reviewer MUST NOT propose new requirements beyond the Unit's completion criteria, MUST NOT redesign the output or reopen the locked spec, and MUST NOT flag stylistic preference. It checks the output against the criteria and files what genuinely fails — nothing more.\n\n\nUnits to audit:\n\n- `shape-unit`\n\n\n\n### 2. adversarial — adversarial reviewer pass + the checks\n\n- Run the full check suite for this station's output — tests, type checks, lints, builds, whatever this station's discipline demands. Run it **completely**, not a subset. A partial or skipped run is not a pass; green means green across the board.\n- If anything fails, it is yours to fix — failures here block the checkpoint. No \"pre-existing\" excuses; if it's red while this station owns the floor, fix it.\n- Adversarially attack the output: where would this break? What did every reviewer miss? Capture the real evidence.\n\n\n\n## The verdict — one state, no partial pass\n\nEvery check and every reviewer lands on exactly one verdict — never a partial, never a \"mostly\":\n\n- **PASS** — you ran it and *observed* it meet the criteria. Not \"looks right\", not \"should work\" — observed.\n- **FAIL** — you ran it and it does not meet the criteria. \"3 of 4 green\" is FAIL until the fourth is green or explicitly explained away.\n- **BLOCKED** — you could not run it. BLOCKED is **not** a PASS; the checkpoint holds until it can run.\n- **SKIP** — genuinely not applicable to this surface, with the reason stated.\n\nWhen in doubt, **FAIL** — a false PASS ships broken work; a false FAIL costs one more look.\n\n## Done when\n\nEvery reviewer has signed off or filed feedback, the full check suite passes, and the evidence is recorded against the station. Filed feedback becomes a fix-worker track — it does **not** get hand-waved past. Record the audit verdict (PASS only when every check passed and every reviewer cleared), then call `darkrun_tick`."
    },
    {
      "seq": 16,
      "track": "run",
      "action_tag": "reflect",
      "station": "shape",
      "prompt": "\n> **Run** `dark-core` · **Station** `shape` · **Phase** `reflect`\n\n> Eliminates: _expensive-structural-reversal_\n\n\n# Reflect — `shape`\n\nThe output is audited and the checks are green. Before the checkpoint gate fires, run an autonomous retrospective. This is the moment to capture what this station taught the run — the learnings that feed the run-level reflections.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`design.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n## Sub-steps\n\n### agentic — autonomous reflection\n\nReflect on this station's pass, on your own, no human in the loop:\n\n- What did manufacturing **shape** reveal that the spec did not anticipate?\n- Where did the work fight back? What was harder, slower, or more fragile than expected?\n- What would you do differently next station? What pattern is worth carrying forward — or avoiding?\n- Did anything here eliminate **expensive-structural-reversal** more (or less) than expected?\n\n- What did the Units teach:\n\n  - `shape-unit`\n\n\n\nRecord the learnings with `darkrun_reflection_record` (pass the `shape` station and your retrospective as the `body`) so they persist on the run and inform later stations — read them back any time with `darkrun_reflection_list`. Be specific and honest — a vague reflection is a wasted one.\n\n## Done when\n\nThe retrospective is captured via `darkrun_reflection_record`. Then call `darkrun_tick` to reach the checkpoint."
    },
    {
      "seq": 17,
      "track": "run",
      "action_tag": "checkpoint",
      "station": "shape",
      "prompt": "\n> **Run** `dark-core` · **Station** `shape` · **Phase** `checkpoint`\n\n> Eliminates: _expensive-structural-reversal_\n\n\n# Checkpoint — `shape`\n\nStation **shape** has passed spec, review, manufacture, audit, and reflect. The gate is now open. Its kind is **`auto`** — that determines who decides whether the station locks.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`design.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\nCheckpoint walks two beats, in order: **brief → user**.\n\n## 1. brief — produce the closing-brief summary\n\nWrite the tight closing brief for whoever holds the decision. It is the durable record of *why this station is allowed to lock*, so make it stand on its own:\n\n- What this station eliminated: **expensive-structural-reversal**.\n- The locked artifact (`design.md`) and where the evidence lives — specs, audit verdict, the green check run.\n- Any concerns reviewers raised and how they were resolved.\n- The retrospective learnings reflect surfaced, if they bear on the lock.\n\nPersist it as the station's **outcome**: call `darkrun_brief_record` with `slug: dark-core`, `station: shape`, `phase: post`, and the closing brief as `body`. This is the durable \"what the station produced\" record the checkpoint surfaces — write it before clearing the gate.\n\n## 2. user — the gate decision (`auto`)\n\nThe gate kind decides *who* clears it. Surface the brief above, then act per the kind:\n\n\n**auto** — no human in the loop. The evidence already justifies the lock. Confirm the criteria are met, lock the station, and call `darkrun_tick` to advance.\n\n\n## Done when\n\nThe gate is cleared per its kind and the station is locked, or the run is held for a decision. Either way, call `darkrun_tick` to record the outcome."
    },
    {
      "seq": 18,
      "track": "run",
      "action_tag": "spec",
      "station": "build",
      "prompt": "\n> **Run** `dark-core` · **Station** `build` · **Phase** `spec`\n\n> Eliminates: _implementation-defects_\n\n\n# Spec — `build`\n\nYou are opening station **build**. Its job is to eliminate a whole class of risk: **implementation-defects**. Nothing downstream is allowed to proceed until that risk is named and bounded here.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`code`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n\n\n**Explorers** (2): `reuse`, `integration_point`\n\n\n**Workers** (4): `test_author` → `builder` → `self_reviewer` → `reconciler`\n\n\n**Reviewers** (2): `correctness`, `maintainability`\n\n\nSpec runs **elaboration and discovery in tandem** — they are NOT two sequential\nsteps. The moment the station opens, kick off both at once: dispatch the explorers\nin parallel *while* you frame the problem. They sharpen each other. Only once both\nhave landed do you decompose.\n\n\n\n## elaborate — frame the problem (concurrently with discovery)\n\nState plainly what this station must achieve to kill **implementation-defects**: the intent, the inputs it inherits from upstream, and the boundary of what is explicitly *out of scope* so later phases don't drift into it. This is the frame the explorers work against — but do NOT wait on a finished frame to start them; the frame and the exploration are written in parallel and inform each other.\n\n## discover — run the explorers in parallel (concurrently with elaboration)\n\nDispatch **all** explorers (`reuse`, `integration_point`) **at once, in parallel** — one subagent each, fanned out concurrently, never one-after-another. Explorers don't build — they surface unknowns, constraints, prior art, and traps. They run alongside your framing; neither blocks the other.\n\n\n\nWhen discovery surfaces a durable project fact worth carrying into **future** runs — a constraint, prior art, a convention, a trap — persist it with **`darkrun_knowledge_record`** (`topic` + `body`). That's the project's shared memory; re-recording a topic updates it. Keep it project-level (cross-run truths), not this run's transient details.\n\n## decompose — once elaboration + discovery have both landed\n\nTurn the framed, explored problem into the smallest set of independently completable **Units** that, together, kill the risk above. A Unit's **body is the spec the executing subagent works from — it gets no other context**. A one-line body is a slug, not a definition; the work that comes back from a thin Unit is thin.\n\nWrite every Unit with `darkrun_unit_create`, with the full anatomy:\n\n- **`body`** — the real definition, in markdown:\n  - the goal: what this Unit produces and why it exists in this station,\n  - **completion criteria, EACH paired with the literal command that verifies it.** Inspect the project's manifest (`Cargo.toml` / `package.json` / `pyproject.toml` / `go.mod` …) *during decompose* and write commands against THIS project's actual stack — never a placeholder.\n    - Good: \"all endpoints return correct status codes (200/400/401/404)\" → `cargo test -p api contracts` exits 0.\n    - Bad: \"API works correctly\", \"tests are written\" — no check, no criterion.\n  - for build-class Units: the **success path, the failure path, and the edge cases** the criteria must cover,\n  - for knowledge/document Units: substantive criteria — what claims the artifact must ground, with sources,\n  - the **files touched** (so review knows the blast radius),\n  - what is explicitly **out of scope** (so the Unit doesn't sprawl).\n- **`depends_on`** — every cross-Unit prerequisite, DECLARED, never left in prose. The wave scheduler sequences **only** on `depends_on`; a dependency mentioned in the body but not declared is invisible — the Unit gets co-scheduled with its own prerequisite and handed inputs that don't exist yet. A body that says \"stub it until unit-X lands\" is the symptom of a missing `depends_on` edge: declare the edge instead of writing the stub.\n- **`inputs` / `outputs`** — the paths consumed and produced. A sibling-produced input path requires that sibling in `depends_on`.\n- **`quality_gates`** — executable `{name, command}` checks proving the criteria. Required for any Unit that declares outputs. Each gate must pass **in the Unit's own isolated worktree at the time it runs** — a gate that needs a sibling's unmerged code, with no `depends_on` edge to order it, is not a gate, it's a Unit scheduled to fail. Circular gates (zero-match `! grep`, prose substrings against the Unit's own output) are rejected.\n- **`model`** — match the tier to the risk: `opus` for architectural, cascading-failure, or deepest-reasoning work, `sonnet` (default) for known patterns plus judgment, `haiku` only for purely mechanical edits.\n\n\n### Units already on record\n\n- `build-unit`\n\nReconcile these against what the explorers found — extend, split, or tighten them; don't blindly accept them.\n\n\n\n\n\n\n## Done when\n\nThe spec names the risk, lists Units with testable completion criteria and dependencies, marks what's out of scope, and it's written to the station's spec artifact. Then call `darkrun_tick`."
    },
    {
      "seq": 19,
      "track": "run",
      "action_tag": "review",
      "station": "build",
      "prompt": "\n> **Run** `dark-core` · **Station** `build` · **Phase** `review`\n\n> Eliminates: _implementation-defects_\n\n\n# Review — `build` spec\n\nBefore a single Unit is manufactured, the spec gets reviewed. A bad spec that reaches manufacture is the most expensive failure in the line — kill it here, cheaply.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`code`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n\n\n**Explorers** (2): `reuse`, `integration_point`\n\n\n**Workers** (4): `test_author` → `builder` → `self_reviewer` → `reconciler`\n\n\n**Reviewers** (2): `correctness`, `maintainability`\n\n\nReview walks three beats, in order: **spec → adversarial → brief**. The operator's decision is a *separate* step the manager surfaces as a gate once this work lands — you do not ask for it inline here.\n\n## 1. spec — verify against the spec\n\nRead the spec produced in the previous phase against its own intent. Before any adversary touches it, confirm it is internally coherent:\n\n- Does it actually name and bound **implementation-defects**, or does it leave a hole?\n- Does every Unit carry testable completion criteria and explicit dependencies?\n- Is the out-of-scope boundary stated, so later phases can't drift into it?\n\n## 2. adversarial — adversarial reviewer pass\n\nDispatch each reviewer (`correctness`, `maintainability`) against the spec. Each owns one lens — let them be ruthless inside it:\n\n\nSome reviewers carry a declared **posture** — honour it when you frame their pass:\n- `correctness` → **strict** (adversarial: find every flaw, assume the spec is wrong until proven otherwise)\n\n\n\n- Does the spec actually eliminate **implementation-defects**, or does it only look like it does?\n- Are the completion criteria testable, or are they wishful?\n- Are the Units genuinely independent, or will they collide during manufacture?\n- Is anything load-bearing left unstated?\n\n**Dispatch the reviewers in parallel** — one subagent each, fanned out concurrently, not one-after-another. They share no state and each owns a different lens, so they run independently. When a reviewer is satisfied it records its own sign-off with **`darkrun_review_stamp`** (`kind: review`, its `role`) — that stamps only its role and returns without advancing the run, so parallel reviewers never contend on the tick. A reviewer that finds a real problem files it with `darkrun_feedback_create` (origin `adversarial_review`) **instead of** stamping. You call `darkrun_tick` once, after every reviewer has returned.\n\n**A reviewer reviews — it does not redesign.** Each reviewer MUST NOT propose new requirements outside the spec's stated intent, MUST NOT substitute its own approach or relitigate a settled tradeoff, and MUST NOT block on stylistic preference. It finds where the spec fails *its own* goal and files exactly that — nothing more.\n\n\n### Units under review\n\n- `build-unit`\n\n\n\nIf a reviewer blocks, fix the spec and re-review — do not advance a spec a reviewer rejected.\n\n## 3. brief — the review summary\n\nProduce a tight brief of the review: which lenses signed off, which filed concerns, and how each concern was resolved (or why it was deferred). This is the record manufacture inherits — it should make the spec's verdict obvious without re-reading every reviewer's notes.\n\nPersist it: call `darkrun_brief_record` with `slug: dark-core`, `station: build`, `phase: pre`, and the brief as `body`. This is the pre-execution brief the operator's review gate surfaces — a durable artifact, not inline prose.\n\n## Done when\n\nEvery reviewer has signed off or filed addressable concerns and the brief is recorded with `darkrun_brief_record`. Then call `darkrun_tick` — the manager opens the operator's pre-execution gate on the next tick. Do not surface the decision inline; the gate does that."
    },
    {
      "seq": 20,
      "track": "run",
      "action_tag": "manufacture",
      "station": "build",
      "prompt": "\n> **Run** `dark-core` · **Station** `build` · **Phase** `manufacture`\n\n> Eliminates: _implementation-defects_\n\n\n# Manufacture — `build`\n\nThis is the build floor. You run the **Pass loop** — _Plan → Make → Challenge → Resolve_ — over the wave-ready Units. The current beat is **test_author**, on model **sonnet**.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`code`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n\n\n**Explorers** (2): `reuse`, `integration_point`\n\n\n**Workers** (4): `test_author` → `builder` → `self_reviewer` → `reconciler`\n\n\n**Reviewers** (2): `correctness`, `maintainability`\n\n\n## This wave\n\n\nDispatch the **test_author** beat in parallel across these wave-ready Units:\n\n- `build-unit`\n\n\n\n\n## Each Unit's spec — the contract the beat works against\n\nThe subagent you dispatch for a Unit gets **no context beyond what you hand it**. Pass the Unit's spec below into its dispatch verbatim — the completion criteria with their verify commands, the declared paths, and the scope boundary are the contract the beat is judged against.\n\n### `build-unit` — build-unit\n\n- **inputs:** `frame.md`, `spec.md`, `design.md`\n\n\n\n\n\n\n\n\n\n\n\n\n## The Pass loop — make → challenge → resolve\n\nThe Pass loop is adversarial on purpose: a single confident pass is exactly where LLM output is most often confidently wrong, so a second pass red-teams the first before anything locks.\n\n- **make** — the worker produces the Unit's output against its completion criteria. Build the real thing, not a sketch.\n- **challenge** — a second pass attacks what make produced: edge cases, missing handling, lazy assumptions. Assume the first pass was optimistic.\n- **resolve** — reconcile make and challenge into a Unit that satisfies its completion criteria with the challenges answered.\n\n\n**Reject routing.** Workers carry a pass-loop role: `self_reviewer` = verify, . A `build` worker produces and repairs; a `verify` worker only judges; a `plan` worker only designs. When a beat **rejects**, bounce back to the **nearest preceding `build` worker** (pass it as `next_worker` to `darkrun_unit_iterate`) — skip `verify`/`plan` beats on the way back, since they can't fix. An `advance` rolls forward to the next worker in order.\n\n\n\n**Quality-gate verifier nonce.** This dispatch carries a one-time verifier token: **`<nonce>`**. When you record a quality gate with `darkrun_quality_gate_record`, pass it as `nonce`. The engine refuses a gate result without the matching token — so a gate is only ever recorded as part of a real verification dispatch, never self-certified. Run the gate's command for real, then record the actual outcome with this nonce.\n\n\nRun **only the `test_author` beat** this tick. When the beat finishes, **record it** with `darkrun_unit_iterate` — pass the `worker`, the `result` (`advance` or `reject`), and a `note`: on advance, what you did and what the next worker needs to know; on reject, why you bounced it (a reject without a reason is refused). That note becomes the next beat's handoff above. Then call `darkrun_tick`; the manager advances the loop or releases the next wave. A Unit is locked only after Resolve and its completion criteria pass.\n\nA Unit gets a **bounded pass budget** — the manager escalates a Unit that can't converge within it to the operator rather than grinding forever. Don't paper over a stuck Unit to dodge the escalation; a Unit that needs more passes than the budget allows is a signal the spec, the scope, or the approach is wrong, and that's the operator's call to make.\n\n\n\n## Done when\n\nThe `test_author` beat is complete for every Unit in this wave and its output is recorded. Then call `darkrun_tick`."
    },
    {
      "seq": 21,
      "track": "run",
      "action_tag": "audit",
      "station": "build",
      "prompt": "\n> **Run** `dark-core` · **Station** `build` · **Phase** `audit`\n\n> Eliminates: _implementation-defects_\n\n\n# Audit — `build` output\n\nThe Units are manufactured. Now audit the *output* against the *spec* **and run the quality checks**. Manufacture proves the thing was built; audit proves the *right* thing was built — and proves it with evidence, not just judgment. (Audit folds in what a separate tests phase used to do: reviewers give judgment, the checks give evidence. Both happen here.)\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`code`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n\n\n**Explorers** (2): `reuse`, `integration_point`\n\n\n**Workers** (4): `test_author` → `builder` → `self_reviewer` → `reconciler`\n\n\n**Reviewers** (2): `correctness`, `maintainability`\n\n\n## Sub-steps\n\nAudit walks two beats, in order:\n\n### 1. spec — verify against the locked spec\n\nDispatch the reviewers (`correctness`, `maintainability`) **in parallel** — one subagent each, concurrently — over the manufactured output. Each asks:\n\n- Does the output meet every Unit's **completion criteria** — not approximately, exactly?\n- Did manufacture drift from the locked spec? Any silent scope creep?\n- Does the combined output actually eliminate **implementation-defects**?\n- Is anything fragile, unhandled, or quietly broken?\n\nWhen a reviewer clears, it records its own approval with **`darkrun_review_stamp`** (`kind: approval`, its `role`) — stamping only its role without advancing the run, so the parallel pass never contends on the tick. A reviewer that finds a real problem files it with `darkrun_feedback_create` **instead of** stamping. You `darkrun_tick` once, after every reviewer returns.\n\n**A reviewer verifies — it does not rebuild.** Each reviewer MUST NOT propose new requirements beyond the Unit's completion criteria, MUST NOT redesign the output or reopen the locked spec, and MUST NOT flag stylistic preference. It checks the output against the criteria and files what genuinely fails — nothing more.\n\n\nUnits to audit:\n\n- `build-unit`\n\n\n\n### 2. adversarial — adversarial reviewer pass + the checks\n\n- Run the full check suite for this station's output — tests, type checks, lints, builds, whatever this station's discipline demands. Run it **completely**, not a subset. A partial or skipped run is not a pass; green means green across the board.\n- If anything fails, it is yours to fix — failures here block the checkpoint. No \"pre-existing\" excuses; if it's red while this station owns the floor, fix it.\n- Adversarially attack the output: where would this break? What did every reviewer miss? Capture the real evidence.\n\n\n\n## The verdict — one state, no partial pass\n\nEvery check and every reviewer lands on exactly one verdict — never a partial, never a \"mostly\":\n\n- **PASS** — you ran it and *observed* it meet the criteria. Not \"looks right\", not \"should work\" — observed.\n- **FAIL** — you ran it and it does not meet the criteria. \"3 of 4 green\" is FAIL until the fourth is green or explicitly explained away.\n- **BLOCKED** — you could not run it. BLOCKED is **not** a PASS; the checkpoint holds until it can run.\n- **SKIP** — genuinely not applicable to this surface, with the reason stated.\n\nWhen in doubt, **FAIL** — a false PASS ships broken work; a false FAIL costs one more look.\n\n## Done when\n\nEvery reviewer has signed off or filed feedback, the full check suite passes, and the evidence is recorded against the station. Filed feedback becomes a fix-worker track — it does **not** get hand-waved past. Record the audit verdict (PASS only when every check passed and every reviewer cleared), then call `darkrun_tick`."
    },
    {
      "seq": 22,
      "track": "run",
      "action_tag": "reflect",
      "station": "build",
      "prompt": "\n> **Run** `dark-core` · **Station** `build` · **Phase** `reflect`\n\n> Eliminates: _implementation-defects_\n\n\n# Reflect — `build`\n\nThe output is audited and the checks are green. Before the checkpoint gate fires, run an autonomous retrospective. This is the moment to capture what this station taught the run — the learnings that feed the run-level reflections.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`code`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n## Sub-steps\n\n### agentic — autonomous reflection\n\nReflect on this station's pass, on your own, no human in the loop:\n\n- What did manufacturing **build** reveal that the spec did not anticipate?\n- Where did the work fight back? What was harder, slower, or more fragile than expected?\n- What would you do differently next station? What pattern is worth carrying forward — or avoiding?\n- Did anything here eliminate **implementation-defects** more (or less) than expected?\n\n- What did the Units teach:\n\n  - `build-unit`\n\n\n\nRecord the learnings with `darkrun_reflection_record` (pass the `build` station and your retrospective as the `body`) so they persist on the run and inform later stations — read them back any time with `darkrun_reflection_list`. Be specific and honest — a vague reflection is a wasted one.\n\n## Done when\n\nThe retrospective is captured via `darkrun_reflection_record`. Then call `darkrun_tick` to reach the checkpoint."
    },
    {
      "seq": 23,
      "track": "run",
      "action_tag": "checkpoint",
      "station": "build",
      "prompt": "\n> **Run** `dark-core` · **Station** `build` · **Phase** `checkpoint`\n\n> Eliminates: _implementation-defects_\n\n\n# Checkpoint — `build`\n\nStation **build** has passed spec, review, manufacture, audit, and reflect. The gate is now open. Its kind is **`auto`** — that determines who decides whether the station locks.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`code`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\nCheckpoint walks two beats, in order: **brief → user**.\n\n## 1. brief — produce the closing-brief summary\n\nWrite the tight closing brief for whoever holds the decision. It is the durable record of *why this station is allowed to lock*, so make it stand on its own:\n\n- What this station eliminated: **implementation-defects**.\n- The locked artifact (`code`) and where the evidence lives — specs, audit verdict, the green check run.\n- Any concerns reviewers raised and how they were resolved.\n- The retrospective learnings reflect surfaced, if they bear on the lock.\n\nPersist it as the station's **outcome**: call `darkrun_brief_record` with `slug: dark-core`, `station: build`, `phase: post`, and the closing brief as `body`. This is the durable \"what the station produced\" record the checkpoint surfaces — write it before clearing the gate.\n\n## 2. user — the gate decision (`auto`)\n\nThe gate kind decides *who* clears it. Surface the brief above, then act per the kind:\n\n\n**auto** — no human in the loop. The evidence already justifies the lock. Confirm the criteria are met, lock the station, and call `darkrun_tick` to advance.\n\n\n## Done when\n\nThe gate is cleared per its kind and the station is locked, or the run is held for a decision. Either way, call `darkrun_tick` to record the outcome."
    },
    {
      "seq": 24,
      "track": "run",
      "action_tag": "spec",
      "station": "prove",
      "prompt": "\n> **Run** `dark-core` · **Station** `prove` · **Phase** `spec`\n\n> Eliminates: _escaped-defects_\n\n\n# Spec — `prove`\n\nYou are opening station **prove**. Its job is to eliminate a whole class of risk: **escaped-defects**. Nothing downstream is allowed to proceed until that risk is named and bounded here.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`proof.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n\n\n**Explorers** (2): `scenario`, `regression`\n\n\n**Workers** (3): `verifier` → `breaker` → `triage`\n\n\n**Reviewers** (2): `evidence`, `coverage`\n\n\nSpec runs **elaboration and discovery in tandem** — they are NOT two sequential\nsteps. The moment the station opens, kick off both at once: dispatch the explorers\nin parallel *while* you frame the problem. They sharpen each other. Only once both\nhave landed do you decompose.\n\n\n\n## elaborate — frame the problem (concurrently with discovery)\n\nState plainly what this station must achieve to kill **escaped-defects**: the intent, the inputs it inherits from upstream, and the boundary of what is explicitly *out of scope* so later phases don't drift into it. This is the frame the explorers work against — but do NOT wait on a finished frame to start them; the frame and the exploration are written in parallel and inform each other.\n\n## discover — run the explorers in parallel (concurrently with elaboration)\n\nDispatch **all** explorers (`scenario`, `regression`) **at once, in parallel** — one subagent each, fanned out concurrently, never one-after-another. Explorers don't build — they surface unknowns, constraints, prior art, and traps. They run alongside your framing; neither blocks the other.\n\n\n\nWhen discovery surfaces a durable project fact worth carrying into **future** runs — a constraint, prior art, a convention, a trap — persist it with **`darkrun_knowledge_record`** (`topic` + `body`). That's the project's shared memory; re-recording a topic updates it. Keep it project-level (cross-run truths), not this run's transient details.\n\n## decompose — once elaboration + discovery have both landed\n\nTurn the framed, explored problem into the smallest set of independently completable **Units** that, together, kill the risk above. A Unit's **body is the spec the executing subagent works from — it gets no other context**. A one-line body is a slug, not a definition; the work that comes back from a thin Unit is thin.\n\nWrite every Unit with `darkrun_unit_create`, with the full anatomy:\n\n- **`body`** — the real definition, in markdown:\n  - the goal: what this Unit produces and why it exists in this station,\n  - **completion criteria, EACH paired with the literal command that verifies it.** Inspect the project's manifest (`Cargo.toml` / `package.json` / `pyproject.toml` / `go.mod` …) *during decompose* and write commands against THIS project's actual stack — never a placeholder.\n    - Good: \"all endpoints return correct status codes (200/400/401/404)\" → `cargo test -p api contracts` exits 0.\n    - Bad: \"API works correctly\", \"tests are written\" — no check, no criterion.\n  - for build-class Units: the **success path, the failure path, and the edge cases** the criteria must cover,\n  - for knowledge/document Units: substantive criteria — what claims the artifact must ground, with sources,\n  - the **files touched** (so review knows the blast radius),\n  - what is explicitly **out of scope** (so the Unit doesn't sprawl).\n- **`depends_on`** — every cross-Unit prerequisite, DECLARED, never left in prose. The wave scheduler sequences **only** on `depends_on`; a dependency mentioned in the body but not declared is invisible — the Unit gets co-scheduled with its own prerequisite and handed inputs that don't exist yet. A body that says \"stub it until unit-X lands\" is the symptom of a missing `depends_on` edge: declare the edge instead of writing the stub.\n- **`inputs` / `outputs`** — the paths consumed and produced. A sibling-produced input path requires that sibling in `depends_on`.\n- **`quality_gates`** — executable `{name, command}` checks proving the criteria. Required for any Unit that declares outputs. Each gate must pass **in the Unit's own isolated worktree at the time it runs** — a gate that needs a sibling's unmerged code, with no `depends_on` edge to order it, is not a gate, it's a Unit scheduled to fail. Circular gates (zero-match `! grep`, prose substrings against the Unit's own output) are rejected.\n- **`model`** — match the tier to the risk: `opus` for architectural, cascading-failure, or deepest-reasoning work, `sonnet` (default) for known patterns plus judgment, `haiku` only for purely mechanical edits.\n\n\n### Units already on record\n\n- `prove-unit`\n\nReconcile these against what the explorers found — extend, split, or tighten them; don't blindly accept them.\n\n\n\n\n\n\n## Done when\n\nThe spec names the risk, lists Units with testable completion criteria and dependencies, marks what's out of scope, and it's written to the station's spec artifact. Then call `darkrun_tick`."
    },
    {
      "seq": 25,
      "track": "run",
      "action_tag": "review",
      "station": "prove",
      "prompt": "\n> **Run** `dark-core` · **Station** `prove` · **Phase** `review`\n\n> Eliminates: _escaped-defects_\n\n\n# Review — `prove` spec\n\nBefore a single Unit is manufactured, the spec gets reviewed. A bad spec that reaches manufacture is the most expensive failure in the line — kill it here, cheaply.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`proof.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n\n\n**Explorers** (2): `scenario`, `regression`\n\n\n**Workers** (3): `verifier` → `breaker` → `triage`\n\n\n**Reviewers** (2): `evidence`, `coverage`\n\n\nReview walks three beats, in order: **spec → adversarial → brief**. The operator's decision is a *separate* step the manager surfaces as a gate once this work lands — you do not ask for it inline here.\n\n## 1. spec — verify against the spec\n\nRead the spec produced in the previous phase against its own intent. Before any adversary touches it, confirm it is internally coherent:\n\n- Does it actually name and bound **escaped-defects**, or does it leave a hole?\n- Does every Unit carry testable completion criteria and explicit dependencies?\n- Is the out-of-scope boundary stated, so later phases can't drift into it?\n\n## 2. adversarial — adversarial reviewer pass\n\nDispatch each reviewer (`evidence`, `coverage`) against the spec. Each owns one lens — let them be ruthless inside it:\n\n\n\n- Does the spec actually eliminate **escaped-defects**, or does it only look like it does?\n- Are the completion criteria testable, or are they wishful?\n- Are the Units genuinely independent, or will they collide during manufacture?\n- Is anything load-bearing left unstated?\n\n**Dispatch the reviewers in parallel** — one subagent each, fanned out concurrently, not one-after-another. They share no state and each owns a different lens, so they run independently. When a reviewer is satisfied it records its own sign-off with **`darkrun_review_stamp`** (`kind: review`, its `role`) — that stamps only its role and returns without advancing the run, so parallel reviewers never contend on the tick. A reviewer that finds a real problem files it with `darkrun_feedback_create` (origin `adversarial_review`) **instead of** stamping. You call `darkrun_tick` once, after every reviewer has returned.\n\n**A reviewer reviews — it does not redesign.** Each reviewer MUST NOT propose new requirements outside the spec's stated intent, MUST NOT substitute its own approach or relitigate a settled tradeoff, and MUST NOT block on stylistic preference. It finds where the spec fails *its own* goal and files exactly that — nothing more.\n\n\n### Units under review\n\n- `prove-unit`\n\n\n\nIf a reviewer blocks, fix the spec and re-review — do not advance a spec a reviewer rejected.\n\n## 3. brief — the review summary\n\nProduce a tight brief of the review: which lenses signed off, which filed concerns, and how each concern was resolved (or why it was deferred). This is the record manufacture inherits — it should make the spec's verdict obvious without re-reading every reviewer's notes.\n\nPersist it: call `darkrun_brief_record` with `slug: dark-core`, `station: prove`, `phase: pre`, and the brief as `body`. This is the pre-execution brief the operator's review gate surfaces — a durable artifact, not inline prose.\n\n## Done when\n\nEvery reviewer has signed off or filed addressable concerns and the brief is recorded with `darkrun_brief_record`. Then call `darkrun_tick` — the manager opens the operator's pre-execution gate on the next tick. Do not surface the decision inline; the gate does that."
    },
    {
      "seq": 26,
      "track": "run",
      "action_tag": "manufacture",
      "station": "prove",
      "prompt": "\n> **Run** `dark-core` · **Station** `prove` · **Phase** `manufacture`\n\n> Eliminates: _escaped-defects_\n\n\n# Manufacture — `prove`\n\nThis is the build floor. You run the **Pass loop** — _Plan → Make → Challenge → Resolve_ — over the wave-ready Units. The current beat is **verifier**, on model **sonnet**.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`proof.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n\n\n**Explorers** (2): `scenario`, `regression`\n\n\n**Workers** (3): `verifier` → `breaker` → `triage`\n\n\n**Reviewers** (2): `evidence`, `coverage`\n\n\n## This wave\n\n\nDispatch the **verifier** beat in parallel across these wave-ready Units:\n\n- `prove-unit`\n\n\n\n\n## Each Unit's spec — the contract the beat works against\n\nThe subagent you dispatch for a Unit gets **no context beyond what you hand it**. Pass the Unit's spec below into its dispatch verbatim — the completion criteria with their verify commands, the declared paths, and the scope boundary are the contract the beat is judged against.\n\n### `prove-unit` — prove-unit\n\n- **inputs:** `spec.md`, `code`\n\n\n\n\n\n\n\n\n\n\n\n\n## The Pass loop — make → challenge → resolve\n\nThe Pass loop is adversarial on purpose: a single confident pass is exactly where LLM output is most often confidently wrong, so a second pass red-teams the first before anything locks.\n\n- **make** — the worker produces the Unit's output against its completion criteria. Build the real thing, not a sketch.\n- **challenge** — a second pass attacks what make produced: edge cases, missing handling, lazy assumptions. Assume the first pass was optimistic.\n- **resolve** — reconcile make and challenge into a Unit that satisfies its completion criteria with the challenges answered.\n\n\n\n\n**Quality-gate verifier nonce.** This dispatch carries a one-time verifier token: **`<nonce>`**. When you record a quality gate with `darkrun_quality_gate_record`, pass it as `nonce`. The engine refuses a gate result without the matching token — so a gate is only ever recorded as part of a real verification dispatch, never self-certified. Run the gate's command for real, then record the actual outcome with this nonce.\n\n\nRun **only the `verifier` beat** this tick. When the beat finishes, **record it** with `darkrun_unit_iterate` — pass the `worker`, the `result` (`advance` or `reject`), and a `note`: on advance, what you did and what the next worker needs to know; on reject, why you bounced it (a reject without a reason is refused). That note becomes the next beat's handoff above. Then call `darkrun_tick`; the manager advances the loop or releases the next wave. A Unit is locked only after Resolve and its completion criteria pass.\n\nA Unit gets a **bounded pass budget** — the manager escalates a Unit that can't converge within it to the operator rather than grinding forever. Don't paper over a stuck Unit to dodge the escalation; a Unit that needs more passes than the budget allows is a signal the spec, the scope, or the approach is wrong, and that's the operator's call to make.\n\n\n\n## Done when\n\nThe `verifier` beat is complete for every Unit in this wave and its output is recorded. Then call `darkrun_tick`."
    },
    {
      "seq": 27,
      "track": "run",
      "action_tag": "audit",
      "station": "prove",
      "prompt": "\n> **Run** `dark-core` · **Station** `prove` · **Phase** `audit`\n\n> Eliminates: _escaped-defects_\n\n\n# Audit — `prove` output\n\nThe Units are manufactured. Now audit the *output* against the *spec* **and run the quality checks**. Manufacture proves the thing was built; audit proves the *right* thing was built — and proves it with evidence, not just judgment. (Audit folds in what a separate tests phase used to do: reviewers give judgment, the checks give evidence. Both happen here.)\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`proof.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n\n\n**Explorers** (2): `scenario`, `regression`\n\n\n**Workers** (3): `verifier` → `breaker` → `triage`\n\n\n**Reviewers** (2): `evidence`, `coverage`\n\n\n## Sub-steps\n\nAudit walks two beats, in order:\n\n### 1. spec — verify against the locked spec\n\nDispatch the reviewers (`evidence`, `coverage`) **in parallel** — one subagent each, concurrently — over the manufactured output. Each asks:\n\n- Does the output meet every Unit's **completion criteria** — not approximately, exactly?\n- Did manufacture drift from the locked spec? Any silent scope creep?\n- Does the combined output actually eliminate **escaped-defects**?\n- Is anything fragile, unhandled, or quietly broken?\n\nWhen a reviewer clears, it records its own approval with **`darkrun_review_stamp`** (`kind: approval`, its `role`) — stamping only its role without advancing the run, so the parallel pass never contends on the tick. A reviewer that finds a real problem files it with `darkrun_feedback_create` **instead of** stamping. You `darkrun_tick` once, after every reviewer returns.\n\n**A reviewer verifies — it does not rebuild.** Each reviewer MUST NOT propose new requirements beyond the Unit's completion criteria, MUST NOT redesign the output or reopen the locked spec, and MUST NOT flag stylistic preference. It checks the output against the criteria and files what genuinely fails — nothing more.\n\n\nUnits to audit:\n\n- `prove-unit`\n\n\n\n### 2. adversarial — adversarial reviewer pass + the checks\n\n- Run the full check suite for this station's output — tests, type checks, lints, builds, whatever this station's discipline demands. Run it **completely**, not a subset. A partial or skipped run is not a pass; green means green across the board.\n- If anything fails, it is yours to fix — failures here block the checkpoint. No \"pre-existing\" excuses; if it's red while this station owns the floor, fix it.\n- Adversarially attack the output: where would this break? What did every reviewer miss? Capture the real evidence.\n\n\n\n## The verdict — one state, no partial pass\n\nEvery check and every reviewer lands on exactly one verdict — never a partial, never a \"mostly\":\n\n- **PASS** — you ran it and *observed* it meet the criteria. Not \"looks right\", not \"should work\" — observed.\n- **FAIL** — you ran it and it does not meet the criteria. \"3 of 4 green\" is FAIL until the fourth is green or explicitly explained away.\n- **BLOCKED** — you could not run it. BLOCKED is **not** a PASS; the checkpoint holds until it can run.\n- **SKIP** — genuinely not applicable to this surface, with the reason stated.\n\nWhen in doubt, **FAIL** — a false PASS ships broken work; a false FAIL costs one more look.\n\n## Done when\n\nEvery reviewer has signed off or filed feedback, the full check suite passes, and the evidence is recorded against the station. Filed feedback becomes a fix-worker track — it does **not** get hand-waved past. Record the audit verdict (PASS only when every check passed and every reviewer cleared), then call `darkrun_tick`."
    },
    {
      "seq": 28,
      "track": "run",
      "action_tag": "reflect",
      "station": "prove",
      "prompt": "\n> **Run** `dark-core` · **Station** `prove` · **Phase** `reflect`\n\n> Eliminates: _escaped-defects_\n\n\n# Reflect — `prove`\n\nThe output is audited and the checks are green. Before the checkpoint gate fires, run an autonomous retrospective. This is the moment to capture what this station taught the run — the learnings that feed the run-level reflections.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`proof.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n## Sub-steps\n\n### agentic — autonomous reflection\n\nReflect on this station's pass, on your own, no human in the loop:\n\n- What did manufacturing **prove** reveal that the spec did not anticipate?\n- Where did the work fight back? What was harder, slower, or more fragile than expected?\n- What would you do differently next station? What pattern is worth carrying forward — or avoiding?\n- Did anything here eliminate **escaped-defects** more (or less) than expected?\n\n- What did the Units teach:\n\n  - `prove-unit`\n\n\n\nRecord the learnings with `darkrun_reflection_record` (pass the `prove` station and your retrospective as the `body`) so they persist on the run and inform later stations — read them back any time with `darkrun_reflection_list`. Be specific and honest — a vague reflection is a wasted one.\n\n## Done when\n\nThe retrospective is captured via `darkrun_reflection_record`. Then call `darkrun_tick` to reach the checkpoint."
    },
    {
      "seq": 29,
      "track": "run",
      "action_tag": "checkpoint",
      "station": "prove",
      "prompt": "\n> **Run** `dark-core` · **Station** `prove` · **Phase** `checkpoint`\n\n> Eliminates: _escaped-defects_\n\n\n# Checkpoint — `prove`\n\nStation **prove** has passed spec, review, manufacture, audit, and reflect. The gate is now open. Its kind is **`auto`** — that determines who decides whether the station locks.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`proof.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\nCheckpoint walks two beats, in order: **brief → user**.\n\n## 1. brief — produce the closing-brief summary\n\nWrite the tight closing brief for whoever holds the decision. It is the durable record of *why this station is allowed to lock*, so make it stand on its own:\n\n- What this station eliminated: **escaped-defects**.\n- The locked artifact (`proof.md`) and where the evidence lives — specs, audit verdict, the green check run.\n- Any concerns reviewers raised and how they were resolved.\n- The retrospective learnings reflect surfaced, if they bear on the lock.\n\nPersist it as the station's **outcome**: call `darkrun_brief_record` with `slug: dark-core`, `station: prove`, `phase: post`, and the closing brief as `body`. This is the durable \"what the station produced\" record the checkpoint surfaces — write it before clearing the gate.\n\n## 2. user — the gate decision (`auto`)\n\nThe gate kind decides *who* clears it. Surface the brief above, then act per the kind:\n\n\n**auto** — no human in the loop. The evidence already justifies the lock. Confirm the criteria are met, lock the station, and call `darkrun_tick` to advance.\n\n\n## Done when\n\nThe gate is cleared per its kind and the station is locked, or the run is held for a decision. Either way, call `darkrun_tick` to record the outcome."
    },
    {
      "seq": 30,
      "track": "run",
      "action_tag": "spec",
      "station": "harden",
      "prompt": "\n> **Run** `dark-core` · **Station** `harden` · **Phase** `spec`\n\n> Eliminates: _works-in-dev-dies-in-prod_\n\n\n# Spec — `harden`\n\nYou are opening station **harden**. Its job is to eliminate a whole class of risk: **works-in-dev-dies-in-prod**. Nothing downstream is allowed to proceed until that risk is named and bounded here.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`release.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n\n\n**Explorers** (2): `threat`, `operability`\n\n\n**Workers** (3): `hardener` → `red_teamer` → `releaser`\n\n\n**Reviewers** (2): `security`, `readiness`\n\n\nSpec runs **elaboration and discovery in tandem** — they are NOT two sequential\nsteps. The moment the station opens, kick off both at once: dispatch the explorers\nin parallel *while* you frame the problem. They sharpen each other. Only once both\nhave landed do you decompose.\n\n\n## Keep or drop — decide at arrival, before any work\n\nThis station is **optional** for this run. Before you elaborate anything, judge whether its risk class — **works-in-dev-dies-in-prod** — actually applies here. If it plainly doesn't (the run is too small to carry the risk, or an upstream artifact already bounds it), drop the station now with `darkrun_station_drop` and the next `darkrun_tick` advances to the following station. The decision is only available **now**: once elaboration or units exist the station has started, and a started station can only be reset, never dropped. Keeping it is the default — drop only when you can say in one sentence why the risk doesn't apply.\n\n\n## elaborate — frame the problem (concurrently with discovery)\n\nState plainly what this station must achieve to kill **works-in-dev-dies-in-prod**: the intent, the inputs it inherits from upstream, and the boundary of what is explicitly *out of scope* so later phases don't drift into it. This is the frame the explorers work against — but do NOT wait on a finished frame to start them; the frame and the exploration are written in parallel and inform each other.\n\n## discover — run the explorers in parallel (concurrently with elaboration)\n\nDispatch **all** explorers (`threat`, `operability`) **at once, in parallel** — one subagent each, fanned out concurrently, never one-after-another. Explorers don't build — they surface unknowns, constraints, prior art, and traps. They run alongside your framing; neither blocks the other.\n\n\n\nWhen discovery surfaces a durable project fact worth carrying into **future** runs — a constraint, prior art, a convention, a trap — persist it with **`darkrun_knowledge_record`** (`topic` + `body`). That's the project's shared memory; re-recording a topic updates it. Keep it project-level (cross-run truths), not this run's transient details.\n\n## decompose — once elaboration + discovery have both landed\n\nTurn the framed, explored problem into the smallest set of independently completable **Units** that, together, kill the risk above. A Unit's **body is the spec the executing subagent works from — it gets no other context**. A one-line body is a slug, not a definition; the work that comes back from a thin Unit is thin.\n\nWrite every Unit with `darkrun_unit_create`, with the full anatomy:\n\n- **`body`** — the real definition, in markdown:\n  - the goal: what this Unit produces and why it exists in this station,\n  - **completion criteria, EACH paired with the literal command that verifies it.** Inspect the project's manifest (`Cargo.toml` / `package.json` / `pyproject.toml` / `go.mod` …) *during decompose* and write commands against THIS project's actual stack — never a placeholder.\n    - Good: \"all endpoints return correct status codes (200/400/401/404)\" → `cargo test -p api contracts` exits 0.\n    - Bad: \"API works correctly\", \"tests are written\" — no check, no criterion.\n  - for build-class Units: the **success path, the failure path, and the edge cases** the criteria must cover,\n  - for knowledge/document Units: substantive criteria — what claims the artifact must ground, with sources,\n  - the **files touched** (so review knows the blast radius),\n  - what is explicitly **out of scope** (so the Unit doesn't sprawl).\n- **`depends_on`** — every cross-Unit prerequisite, DECLARED, never left in prose. The wave scheduler sequences **only** on `depends_on`; a dependency mentioned in the body but not declared is invisible — the Unit gets co-scheduled with its own prerequisite and handed inputs that don't exist yet. A body that says \"stub it until unit-X lands\" is the symptom of a missing `depends_on` edge: declare the edge instead of writing the stub.\n- **`inputs` / `outputs`** — the paths consumed and produced. A sibling-produced input path requires that sibling in `depends_on`.\n- **`quality_gates`** — executable `{name, command}` checks proving the criteria. Required for any Unit that declares outputs. Each gate must pass **in the Unit's own isolated worktree at the time it runs** — a gate that needs a sibling's unmerged code, with no `depends_on` edge to order it, is not a gate, it's a Unit scheduled to fail. Circular gates (zero-match `! grep`, prose substrings against the Unit's own output) are rejected.\n- **`model`** — match the tier to the risk: `opus` for architectural, cascading-failure, or deepest-reasoning work, `sonnet` (default) for known patterns plus judgment, `haiku` only for purely mechanical edits.\n\n\n### Units already on record\n\n- `harden-unit`\n\nReconcile these against what the explorers found — extend, split, or tighten them; don't blindly accept them.\n\n\n\n\n\n\n## Done when\n\nThe spec names the risk, lists Units with testable completion criteria and dependencies, marks what's out of scope, and it's written to the station's spec artifact. Then call `darkrun_tick`."
    },
    {
      "seq": 31,
      "track": "run",
      "action_tag": "review",
      "station": "harden",
      "prompt": "\n> **Run** `dark-core` · **Station** `harden` · **Phase** `review`\n\n> Eliminates: _works-in-dev-dies-in-prod_\n\n\n# Review — `harden` spec\n\nBefore a single Unit is manufactured, the spec gets reviewed. A bad spec that reaches manufacture is the most expensive failure in the line — kill it here, cheaply.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`release.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n\n\n**Explorers** (2): `threat`, `operability`\n\n\n**Workers** (3): `hardener` → `red_teamer` → `releaser`\n\n\n**Reviewers** (2): `security`, `readiness`\n\n\nReview walks three beats, in order: **spec → adversarial → brief**. The operator's decision is a *separate* step the manager surfaces as a gate once this work lands — you do not ask for it inline here.\n\n## 1. spec — verify against the spec\n\nRead the spec produced in the previous phase against its own intent. Before any adversary touches it, confirm it is internally coherent:\n\n- Does it actually name and bound **works-in-dev-dies-in-prod**, or does it leave a hole?\n- Does every Unit carry testable completion criteria and explicit dependencies?\n- Is the out-of-scope boundary stated, so later phases can't drift into it?\n\n## 2. adversarial — adversarial reviewer pass\n\nDispatch each reviewer (`security`, `readiness`) against the spec. Each owns one lens — let them be ruthless inside it:\n\n\n\n- Does the spec actually eliminate **works-in-dev-dies-in-prod**, or does it only look like it does?\n- Are the completion criteria testable, or are they wishful?\n- Are the Units genuinely independent, or will they collide during manufacture?\n- Is anything load-bearing left unstated?\n\n**Dispatch the reviewers in parallel** — one subagent each, fanned out concurrently, not one-after-another. They share no state and each owns a different lens, so they run independently. When a reviewer is satisfied it records its own sign-off with **`darkrun_review_stamp`** (`kind: review`, its `role`) — that stamps only its role and returns without advancing the run, so parallel reviewers never contend on the tick. A reviewer that finds a real problem files it with `darkrun_feedback_create` (origin `adversarial_review`) **instead of** stamping. You call `darkrun_tick` once, after every reviewer has returned.\n\n**A reviewer reviews — it does not redesign.** Each reviewer MUST NOT propose new requirements outside the spec's stated intent, MUST NOT substitute its own approach or relitigate a settled tradeoff, and MUST NOT block on stylistic preference. It finds where the spec fails *its own* goal and files exactly that — nothing more.\n\n\n### Units under review\n\n- `harden-unit`\n\n\n\nIf a reviewer blocks, fix the spec and re-review — do not advance a spec a reviewer rejected.\n\n## 3. brief — the review summary\n\nProduce a tight brief of the review: which lenses signed off, which filed concerns, and how each concern was resolved (or why it was deferred). This is the record manufacture inherits — it should make the spec's verdict obvious without re-reading every reviewer's notes.\n\nPersist it: call `darkrun_brief_record` with `slug: dark-core`, `station: harden`, `phase: pre`, and the brief as `body`. This is the pre-execution brief the operator's review gate surfaces — a durable artifact, not inline prose.\n\n## Done when\n\nEvery reviewer has signed off or filed addressable concerns and the brief is recorded with `darkrun_brief_record`. Then call `darkrun_tick` — the manager opens the operator's pre-execution gate on the next tick. Do not surface the decision inline; the gate does that."
    },
    {
      "seq": 32,
      "track": "run",
      "action_tag": "manufacture",
      "station": "harden",
      "prompt": "\n> **Run** `dark-core` · **Station** `harden` · **Phase** `manufacture`\n\n> Eliminates: _works-in-dev-dies-in-prod_\n\n\n# Manufacture — `harden`\n\nThis is the build floor. You run the **Pass loop** — _Plan → Make → Challenge → Resolve_ — over the wave-ready Units. The current beat is **hardener**, on model **sonnet**.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`release.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n\n\n**Explorers** (2): `threat`, `operability`\n\n\n**Workers** (3): `hardener` → `red_teamer` → `releaser`\n\n\n**Reviewers** (2): `security`, `readiness`\n\n\n## This wave\n\n\nDispatch the **hardener** beat in parallel across these wave-ready Units:\n\n- `harden-unit`\n\n\n\n\n## Each Unit's spec — the contract the beat works against\n\nThe subagent you dispatch for a Unit gets **no context beyond what you hand it**. Pass the Unit's spec below into its dispatch verbatim — the completion criteria with their verify commands, the declared paths, and the scope boundary are the contract the beat is judged against.\n\n### `harden-unit` — harden-unit\n\n- **inputs:** `frame.md`, `spec.md`, `design.md`, `proof.md`, `code`\n\n\n\n\n\n\n\n\n\n\n\n\n## The Pass loop — make → challenge → resolve\n\nThe Pass loop is adversarial on purpose: a single confident pass is exactly where LLM output is most often confidently wrong, so a second pass red-teams the first before anything locks.\n\n- **make** — the worker produces the Unit's output against its completion criteria. Build the real thing, not a sketch.\n- **challenge** — a second pass attacks what make produced: edge cases, missing handling, lazy assumptions. Assume the first pass was optimistic.\n- **resolve** — reconcile make and challenge into a Unit that satisfies its completion criteria with the challenges answered.\n\n\n\n\n**Quality-gate verifier nonce.** This dispatch carries a one-time verifier token: **`<nonce>`**. When you record a quality gate with `darkrun_quality_gate_record`, pass it as `nonce`. The engine refuses a gate result without the matching token — so a gate is only ever recorded as part of a real verification dispatch, never self-certified. Run the gate's command for real, then record the actual outcome with this nonce.\n\n\nRun **only the `hardener` beat** this tick. When the beat finishes, **record it** with `darkrun_unit_iterate` — pass the `worker`, the `result` (`advance` or `reject`), and a `note`: on advance, what you did and what the next worker needs to know; on reject, why you bounced it (a reject without a reason is refused). That note becomes the next beat's handoff above. Then call `darkrun_tick`; the manager advances the loop or releases the next wave. A Unit is locked only after Resolve and its completion criteria pass.\n\nA Unit gets a **bounded pass budget** — the manager escalates a Unit that can't converge within it to the operator rather than grinding forever. Don't paper over a stuck Unit to dodge the escalation; a Unit that needs more passes than the budget allows is a signal the spec, the scope, or the approach is wrong, and that's the operator's call to make.\n\n\n\n## Done when\n\nThe `hardener` beat is complete for every Unit in this wave and its output is recorded. Then call `darkrun_tick`."
    },
    {
      "seq": 33,
      "track": "run",
      "action_tag": "audit",
      "station": "harden",
      "prompt": "\n> **Run** `dark-core` · **Station** `harden` · **Phase** `audit`\n\n> Eliminates: _works-in-dev-dies-in-prod_\n\n\n# Audit — `harden` output\n\nThe Units are manufactured. Now audit the *output* against the *spec* **and run the quality checks**. Manufacture proves the thing was built; audit proves the *right* thing was built — and proves it with evidence, not just judgment. (Audit folds in what a separate tests phase used to do: reviewers give judgment, the checks give evidence. Both happen here.)\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`release.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n\n\n**Explorers** (2): `threat`, `operability`\n\n\n**Workers** (3): `hardener` → `red_teamer` → `releaser`\n\n\n**Reviewers** (2): `security`, `readiness`\n\n\n## Sub-steps\n\nAudit walks two beats, in order:\n\n### 1. spec — verify against the locked spec\n\nDispatch the reviewers (`security`, `readiness`) **in parallel** — one subagent each, concurrently — over the manufactured output. Each asks:\n\n- Does the output meet every Unit's **completion criteria** — not approximately, exactly?\n- Did manufacture drift from the locked spec? Any silent scope creep?\n- Does the combined output actually eliminate **works-in-dev-dies-in-prod**?\n- Is anything fragile, unhandled, or quietly broken?\n\nWhen a reviewer clears, it records its own approval with **`darkrun_review_stamp`** (`kind: approval`, its `role`) — stamping only its role without advancing the run, so the parallel pass never contends on the tick. A reviewer that finds a real problem files it with `darkrun_feedback_create` **instead of** stamping. You `darkrun_tick` once, after every reviewer returns.\n\n**A reviewer verifies — it does not rebuild.** Each reviewer MUST NOT propose new requirements beyond the Unit's completion criteria, MUST NOT redesign the output or reopen the locked spec, and MUST NOT flag stylistic preference. It checks the output against the criteria and files what genuinely fails — nothing more.\n\n\nUnits to audit:\n\n- `harden-unit`\n\n\n\n### 2. adversarial — adversarial reviewer pass + the checks\n\n- Run the full check suite for this station's output — tests, type checks, lints, builds, whatever this station's discipline demands. Run it **completely**, not a subset. A partial or skipped run is not a pass; green means green across the board.\n- If anything fails, it is yours to fix — failures here block the checkpoint. No \"pre-existing\" excuses; if it's red while this station owns the floor, fix it.\n- Adversarially attack the output: where would this break? What did every reviewer miss? Capture the real evidence.\n\n\n\n## The verdict — one state, no partial pass\n\nEvery check and every reviewer lands on exactly one verdict — never a partial, never a \"mostly\":\n\n- **PASS** — you ran it and *observed* it meet the criteria. Not \"looks right\", not \"should work\" — observed.\n- **FAIL** — you ran it and it does not meet the criteria. \"3 of 4 green\" is FAIL until the fourth is green or explicitly explained away.\n- **BLOCKED** — you could not run it. BLOCKED is **not** a PASS; the checkpoint holds until it can run.\n- **SKIP** — genuinely not applicable to this surface, with the reason stated.\n\nWhen in doubt, **FAIL** — a false PASS ships broken work; a false FAIL costs one more look.\n\n## Done when\n\nEvery reviewer has signed off or filed feedback, the full check suite passes, and the evidence is recorded against the station. Filed feedback becomes a fix-worker track — it does **not** get hand-waved past. Record the audit verdict (PASS only when every check passed and every reviewer cleared), then call `darkrun_tick`."
    },
    {
      "seq": 34,
      "track": "run",
      "action_tag": "reflect",
      "station": "harden",
      "prompt": "\n> **Run** `dark-core` · **Station** `harden` · **Phase** `reflect`\n\n> Eliminates: _works-in-dev-dies-in-prod_\n\n\n# Reflect — `harden`\n\nThe output is audited and the checks are green. Before the checkpoint gate fires, run an autonomous retrospective. This is the moment to capture what this station taught the run — the learnings that feed the run-level reflections.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`release.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\n## Sub-steps\n\n### agentic — autonomous reflection\n\nReflect on this station's pass, on your own, no human in the loop:\n\n- What did manufacturing **harden** reveal that the spec did not anticipate?\n- Where did the work fight back? What was harder, slower, or more fragile than expected?\n- What would you do differently next station? What pattern is worth carrying forward — or avoiding?\n- Did anything here eliminate **works-in-dev-dies-in-prod** more (or less) than expected?\n\n- What did the Units teach:\n\n  - `harden-unit`\n\n\n\nRecord the learnings with `darkrun_reflection_record` (pass the `harden` station and your retrospective as the `body`) so they persist on the run and inform later stations — read them back any time with `darkrun_reflection_list`. Be specific and honest — a vague reflection is a wasted one.\n\n## Done when\n\nThe retrospective is captured via `darkrun_reflection_record`. Then call `darkrun_tick` to reach the checkpoint."
    },
    {
      "seq": 35,
      "track": "run",
      "action_tag": "checkpoint",
      "station": "harden",
      "prompt": "\n> **Run** `dark-core` · **Station** `harden` · **Phase** `checkpoint`\n\n> Eliminates: _works-in-dev-dies-in-prod_\n\n\n# Checkpoint — `harden`\n\nStation **harden** has passed spec, review, manufacture, audit, and reflect. The gate is now open. Its kind is **`auto`** — that determines who decides whether the station locks.\n\n\n**Contract**\n\n- Do exactly the work this action describes — no more, no less. Don't skip ahead to a later phase.\n- Treat the locked artifact (`release.md`) as the source of truth. Read it before you act; never silently rewrite a locked decision.\n- Every claim you make must be backed by something you actually ran, read, or wrote. No assumed results.\n- Be specific and committed. **No placeholders** — a `TBD`, `similar to …`, `add error handling`, `etc.`, or `…` is a hole, not a decision; name the actual, checkable condition. **No hedging** — when you report work done, use a verb of completed action (`added`, `implemented`, `fixed`), never `should`, `seems`, `probably`, `might`, or `looks like`. Hedging is the tell of unfinished work.\n- When the action is finished, record your output where the station expects it, then call `darkrun_tick` again for the next instruction. The manager — not you — decides what comes next.\n\nCheckpoint walks two beats, in order: **brief → user**.\n\n## 1. brief — produce the closing-brief summary\n\nWrite the tight closing brief for whoever holds the decision. It is the durable record of *why this station is allowed to lock*, so make it stand on its own:\n\n- What this station eliminated: **works-in-dev-dies-in-prod**.\n- The locked artifact (`release.md`) and where the evidence lives — specs, audit verdict, the green check run.\n- Any concerns reviewers raised and how they were resolved.\n- The retrospective learnings reflect surfaced, if they bear on the lock.\n\nPersist it as the station's **outcome**: call `darkrun_brief_record` with `slug: dark-core`, `station: harden`, `phase: post`, and the closing brief as `body`. This is the durable \"what the station produced\" record the checkpoint surfaces — write it before clearing the gate.\n\n## 2. user — the gate decision (`auto`)\n\nThe gate kind decides *who* clears it. Surface the brief above, then act per the kind:\n\n\n**auto** — no human in the loop. The evidence already justifies the lock. Confirm the criteria are met, lock the station, and call `darkrun_tick` to advance.\n\n\n## Done when\n\nThe gate is cleared per its kind and the station is locked, or the run is held for a decision. Either way, call `darkrun_tick` to record the outcome."
    },
    {
      "seq": 36,
      "track": "run",
      "action_tag": "sealed",
      "station": null,
      "prompt": "\n> **Run** `dark-core` · **Phase** `sealed`\n\n\n# Sealed — `dark-core`\n\nThe run is **sealed**. Every station is locked, the run-level review passed, and reflections are recorded. There is no further work to dispatch.\n\n## What this means\n\n- All locked artifacts are final. Touching one now is **drift** and will reopen the run.\n- The evidence trail — specs, audits, test results, reflections — stands as the record of what was built and why.\n\n\n\n## What to do\n\nNothing further on this run. If new work is needed, start a new run rather than reopening this one. Report the seal to the operator with a short summary of what shipped."
    }
  ],
  "events": [
    {
      "seq": 0,
      "event": "darkrun.run.created",
      "fields": {
        "at": "<normalized>",
        "factory": "software",
        "mode": "dark",
        "run": "dark-core"
      }
    },
    {
      "seq": 1,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "spec",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "frame"
      }
    },
    {
      "seq": 2,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "review",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "frame"
      }
    },
    {
      "seq": 3,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "manufacture",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "frame"
      }
    },
    {
      "seq": 4,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "audit",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "frame"
      }
    },
    {
      "seq": 5,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "reflect",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "frame"
      }
    },
    {
      "seq": 6,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "checkpoint",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "frame"
      }
    },
    {
      "seq": 7,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "spec",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "specify"
      }
    },
    {
      "seq": 8,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "review",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "specify"
      }
    },
    {
      "seq": 9,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "manufacture",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "specify"
      }
    },
    {
      "seq": 10,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "audit",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "specify"
      }
    },
    {
      "seq": 11,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "reflect",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "specify"
      }
    },
    {
      "seq": 12,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "checkpoint",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "specify"
      }
    },
    {
      "seq": 13,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "spec",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "shape"
      }
    },
    {
      "seq": 14,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "review",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "shape"
      }
    },
    {
      "seq": 15,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "manufacture",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "shape"
      }
    },
    {
      "seq": 16,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "audit",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "shape"
      }
    },
    {
      "seq": 17,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "reflect",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "shape"
      }
    },
    {
      "seq": 18,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "checkpoint",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "shape"
      }
    },
    {
      "seq": 19,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "spec",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "build"
      }
    },
    {
      "seq": 20,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "review",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "build"
      }
    },
    {
      "seq": 21,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "manufacture",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "build"
      }
    },
    {
      "seq": 22,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "audit",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "build"
      }
    },
    {
      "seq": 23,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "reflect",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "build"
      }
    },
    {
      "seq": 24,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "checkpoint",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "build"
      }
    },
    {
      "seq": 25,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "spec",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "prove"
      }
    },
    {
      "seq": 26,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "review",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "prove"
      }
    },
    {
      "seq": 27,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "manufacture",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "prove"
      }
    },
    {
      "seq": 28,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "audit",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "prove"
      }
    },
    {
      "seq": 29,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "reflect",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "prove"
      }
    },
    {
      "seq": 30,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "checkpoint",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "prove"
      }
    },
    {
      "seq": 31,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "spec",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "harden"
      }
    },
    {
      "seq": 32,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "review",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "harden"
      }
    },
    {
      "seq": 33,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "manufacture",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "harden"
      }
    },
    {
      "seq": 34,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "audit",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "harden"
      }
    },
    {
      "seq": 35,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "reflect",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "harden"
      }
    },
    {
      "seq": 36,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "checkpoint",
        "at": "<normalized>",
        "run": "dark-core",
        "station": "harden"
      }
    },
    {
      "seq": 37,
      "event": "darkrun.manager.action",
      "fields": {
        "action": "sealed",
        "at": "<normalized>",
        "run": "dark-core",
        "station": null
      }
    }
  ],
  "units": [
    {
      "slug": "build-unit",
      "station": "build",
      "depends_on": [],
      "status": "completed"
    },
    {
      "slug": "frame-unit",
      "station": "frame",
      "depends_on": [],
      "status": "completed"
    },
    {
      "slug": "harden-unit",
      "station": "harden",
      "depends_on": [],
      "status": "completed"
    },
    {
      "slug": "prove-unit",
      "station": "prove",
      "depends_on": [],
      "status": "completed"
    },
    {
      "slug": "shape-unit",
      "station": "shape",
      "depends_on": [],
      "status": "completed"
    },
    {
      "slug": "specify-unit",
      "station": "specify",
      "depends_on": [],
      "status": "completed"
    }
  ]
}
