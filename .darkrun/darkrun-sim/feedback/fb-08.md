---
status: pending
station: specify
severity: high
origin: discovery
created_at: 2026-07-12T05:42:19.391631+00:00
---
Build-station discovery found a spec-internal gap in the locked spec.md: Contract 3's SimFixture schema carries no unit-level data (FixtureTick is {seq, track, action_tag, station, prompt}; FixtureEvent fields for units are limited to darkrun.unit.completed's {unit, station}, and the scripted scenario's unit writes go through store.write_unit directly, bypassing event emission entirely per crates/darkrun-sim/src/harness.rs lines 97-128 kept verbatim by Contract 4), yet AC-13 requires the replay page to render UnitGraph — Vec<UnitGraphNode> plus Vec<GraphEdge>, a real DAG — "against data derived from the embedded fixture." As specified there is no fixture field to source unit nodes or edges from; Build would ship a degenerate empty graph that passes AC-13's grep while showing nothing. Operator decision (2026-07-11, this session): close the gap by AMENDING Contract 3 with one additive field — units: Vec<FixtureUnit> where FixtureUnit is {slug: String, station: String, depends_on: Vec<String>, status: String}, captured from the world's StateStore (read_units) once at end-of-run, after the terminal tick, normalized per the existing rules (no timestamps carried). AC-13's wording updates to name units[] as UnitGraph's data source. The fix is additive only: no existing field changes, the schema stays wasm-safe serde in darkrun-core, and no other AC or contract is touched.
