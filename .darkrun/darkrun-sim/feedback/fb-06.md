---
status: pending
station: specify
severity: high
origin: run_review
created_at: 2026-07-11T18:38:33.052546+00:00
---
Testability Audit (role: testability) of `.darkrun/darkrun-sim/specify/spec.md` — FAIL.

AC-5 ("The scripted provider is structurally prompt-blind"), Check clause, spec.md lines 45: the check's first branch treats naming the parameter `_prompt` as self-evidently proving the claim, calling it "compiler-enforced unused" (line 43: "the parameter named `_prompt` (compiler-enforced unused)"). This is factually wrong about Rust: an underscore-prefixed parameter name only suppresses the `unused_variables` lint — it does NOT prevent the body from reading or branching on the parameter. Verified directly:

```rust
fn next_move(_prompt: Option<&str>) -> bool {
    if let Some(p) = _prompt {
        if p.contains("station") { return true; }
    }
    false
}
```

compiles with zero errors and zero warnings (checked with `rustc -W unused`), and `_prompt.contains(...)` is exercised at runtime. Nothing about the compiler stops this.

Concrete failure scenario: an implementation of the scripted `Provider` renames its parameter to `_prompt` while still branching on its content (e.g. `if _prompt.unwrap_or_default().contains("station")`), directly violating AC-5's claim ("never inspects it: no `if`, `match`, `.contains(`, or other conditional keyed on its content"). Run AC-5's check exactly as written: `grep -n 'fn next_move' -A 5 <provider module>` shows the parameter named `_prompt`, which the check's own OR-logic accepts as sufficient proof (first branch) without ever running the content-scan (`grep -c 'prompt' ...`) that the check only prescribes for the "if named `prompt`" case. The check reports PASS while the claim it's supposed to gate is FALSE.

Fix direction (not prescribing exact wording, just the defect to close): drop the "compiler-enforced unused" framing and its naming-based escape hatch entirely; require the content-scan grep (or an equivalent `#[test]`/clippy-lint-based check) unconditionally, regardless of what the parameter is named, since Rust provides no naming convention that mechanically prevents inspection of a parameter's value.

Everything else audited passed: all six unit completion criteria (non-empty; exact heading order; 16 ACs >= 12; 32 resolving citations; zero haiku hits; zero hedge/time-language hits — all verified live from the checkout, which matches the `darkrun/darkrun-sim/specify` branch copy via `git show ... | diff` with a clean exit); the AC-9/AC-10/AC-3 tightener resolutions were spot-checked against Contract 1 and the Edge Cases section and are internally consistent, no residual contradiction; multiple line-number citations (route.rs:124, preview.rs:88-93, review.rs:199, ci.yml:149, deadlock.rs HALT_THRESHOLD=4/STALE_AGE_SECS=3600, hosting.rs's 3 non-defaulted + 4 defaulted methods, darkrun-core's cfg(unix)-only nix dep, darkrun-site's current zero darkrun-mcp tree edge) were spot-verified against the actual repo state and are accurate.

Not stamping approval; filing this instead per audit instructions since a real, concrete testability failure exists in AC-5.
