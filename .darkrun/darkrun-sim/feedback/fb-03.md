---
status: addressed
station: frame
severity: blocker
origin: discovery
created_at: 2026-07-11T05:45:57.344429+00:00
---
frame.md remains absent from the station branch after fb-02: land_fix fired inside fb-02's resolve, but the fix_feedback tick that dispatched fb-02 had already re-run the per-tick downstream sync (run-main -> station), which re-conflicted the station checkout on engine state files (AA add/add on action-log.jsonl, deadlock.json, events.jsonl, feedback/fb-01.md), so merge_site refused the one-shot land again. Root cause pattern: every tick's sync re-dirties the station checkout, and every land (unit, fix) is one-shot and fires either on a tick (land_unit at Audit) or immediately after one (land_fix at resolve), so the land always loses the race. This time the sequence is inverted to win it: the station sync merge is completed and committed (station tip 48ee17c, engine state resolved to run-main's copies, checkout verified clean with zero status entries) BEFORE this feedback's fix is resolved, and no tick occurs between the checkout cleanup and the resolve. Fix for this item: materialize .darkrun/darkrun-sim/frame/frame.md in this feedback's fix worktree, byte-identical to the unit branch's distilled gate-passing artifact (5dd9585, same content as fix commits 2911e10 and ae33de2), commit, resolve — land_fix then merges into the clean station checkout.
